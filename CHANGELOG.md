# CHANGELOG — qpedia.ir
### گزارش کامل تغییرات · ۳ شهریور ۱۴۰۴ / ۲۵ اوت ۲۰۲۶

> هر عدد در این سند با درخواست زنده به سایت تأیید شده است، نه از حافظه.

---

## خلاصهٔ یک‌نگاه

| شاخص | پیش | پس | تغییر |
|---|---|---|---|
| افزونه‌های فعال | ۱۶ | ۸ | −۵۰٪ |
| تاکسونومی‌های موازی | ۶ | ۲ | −۶۷٪ |
| نوع محتوا | ۳ (مقاله/اصطلاح/دانشمند) | ۲ | ادغام شد |
| مقالات | ۴۰ | **۵۹** | +۱۹ |
| ترم‌های خالی | ۳۴ | ۱ | −۹۷٪ |
| مقالات بدون دسته | ۵۹ | **۰** | کامل |
| اولین بایت صفحهٔ اصلی | ۲٫۲ ثانیه | **۰٫۲۷ ثانیه** | ۸× سریع‌تر |
| ساختار در پوسته | ۰٪ | **۱۰۰٪** | مستقل از افزونه |

---

## ۱ · محتوا و ساختار

### ۱.۱ ادغام اصطلاحات در مقالات
پست‌تایپ `quantum_term` **حذف شد**. تأیید زنده: `quantum_term = 0`، `quantum_article = 59`.

- **۱۹ اصطلاح** به مقاله تبدیل شدند (ID، تاریخ، محتوا و لینک‌های داخلی دست‌نخورده)
- **۵ اصطلاح** که مقالهٔ هم‌نام داشتند حذف و ۳۰۱ شدند:

| از | به |
|---|---|
| `/glossary/qubit/` | `/qubit/` |
| `/glossary/decoherence/` | `/decoherence/` |
| `/glossary/uncertainty-principle/` | `/uncertainty-principle/` |
| `/glossary/quantum-superposition/` | `/quantum-superposition/` |
| `/glossary/schrodingers-cat/` | `/schrodinger-cat/` |

> **یافتهٔ کلیدی:** اصطلاحات به‌طور میانگین **۴۸۰۰ نویسه** بودند — یعنی از قبل مقالهٔ کامل. تبدیلشان نیازی به بازنویسی نداشت.

### ۱.۲ تاکسونومی واحد `quantum_category`
جایگزین ۴ تاکسونومی موازی. **۷ دستهٔ اصلی + ۷ زیردسته**، آدرس `/topic/{slug}/`.

| دسته | مقاله | زیردسته‌ها |
|---|---|---|
| مبانی و مفاهیم | ۲۱ | مفاهیم پایه ۱۹ · ذرات بنیادی ۲ |
| فناوری و کاربردها | ۱۰ | روزمره ۴ · رایانش ۲ · زیست‌شناسی ۴ |
| تاریخ و آزمایش‌ها | ۸ | تاریخ ۴ · آزمایش‌ها ۴ |
| پدیده‌های کوانتومی | ۷ | — |
| ریاضیات و فرمالیسم | ۷ | — |
| نقد شبه‌علم | ۴ | — |
| تفسیرها و فلسفه | ۲ | — |

**۵۹ از ۵۹ مقاله منتسب. صفر مقالهٔ بی‌دسته. صفر دستهٔ خالی.**

### ۱.۳ پاکسازی
- ۲۳ برچسب خالی حذف
- ۱۰ دستهٔ خالی حذف (۱ پیش‌فرض وردپرس، حذف‌نشدنی)
- دستهٔ `ntanglement` (غلط تایپی) حذف
- اسلاگ فارسی URL-encoded «منحنی لیساژو» → `lissajous-curve` با ۳۰۱
- جابه‌جایی اسلاگ‌های `quantum-state` / `wave-function-term` اصلاح شد
- `readme.html` حذف (`license.txt` هنوز باز است)

---

## ۲ · انتقال ساختار به پوستهٔ فرزند

**اصل حاکم — دستور کارفرما:** ساختار و ظاهر در **پوسته**؛ افزونه فقط برای کارهای **یک‌بارمصرف**.

منتقل‌شده به `quantum-pedia-child/functions.php`:

| بخش | کار |
|---|---|
| ۱ | ثبت تاکسونومی `quantum_category` |
| ۲ | تازه‌سازی خودکار قوانین بازنویسی (خودخاموش‌شونده) |
| ۳ | بارگذاری CSS با `wp_enqueue_style` + نسخه‌گذاری `filemtime` |
| ۴ | نمایش مقالات در صفحهٔ اصلی، جست‌وجو و آرشیو تاریخ |
| ۵ | منوی خودکار از دسته‌ها |
| ۶ | هاب دسته‌ها در صفحهٔ اصلی |
| ۷ | حذف سایدبار خالی |
| ۸ | هدر ثابت با `wp_body_open` |
| ۹ | فوتر ثابت با `wp_footer` |

**CSS:** `assets/css/qpedia-layouts.css` — ۲۵۴ خط، بدون استایل درون‌خطی.

---

## ۳ · افزونه‌ها

### حذف‌شده (۸)
`Qpedia Restructure` · `Assign Categories` · `Seed` · `Scientists` · `Scientist Bodies` · `Scientists Fix` · `Header Fix` · `UI` · `Shell`

### باقی‌مانده (۸)

| افزونه | وضعیت |
|---|---|
| **Qpedia Fixes** | 🔴 حیاتی — ثبت پست‌تایپ‌ها |
| **Qpedia Architecture** | 🔴 حیاتی — ساختار آدرس و ۳۰۱ |
| **LiteSpeed Cache** | 🔴 سرعت |
| **UpdraftPlus** | 🔴 پشتیبان |
| Qpedia Redirect Fix | 🟡 قابل انتقال به `.htaccess` |
| Qpedia Home Feed | 🟡 **افزونهٔ اضافی — کارش در پوسته هست** |
| Qpedia Article Template | 🟡 قابل انتقال به قالب |
| Qpedia Quantum Glossary | 🟡 اگر شورت‌کد `[qterm]` استفاده نشده، حذف شود |

---

## ۴ · robots.txt

- ۹ خزندهٔ AI از حالت `Disallow` خارج شدند
- `Disallow: /*?*` حذف شد — این خط هر لینک با `?utm_source=` یا `?fbclid=` را از ایندکس خارج می‌کرد
- دو بلوک تکراری `User-agent: *` ادغام شد

> **یافتهٔ مهم:** آزمون با پنج هویت جعلی نشان داد **هیچ خزنده‌ای واقعاً مسدود نبود** — همه HTTP 200 می‌گرفتند. `robots.txt` درخواست است، نه قفل.

---

## ۵ · عملکرد

```
پیش : ۲٫۲۰ ثانیه (پنج اندازه‌گیری پیاپی، پایدار)
پس  : ۰٫۲۷ ثانیه
```

علت: حذف ۳۴ ترم خالی، ادغام دو پست‌تایپ، حذف افزونه‌های سنگین.

---

## ۶ · قواعد ثبت‌شده

### بند ۵.۱ — روایت به‌جای نقل‌قول
هیچ جملهٔ مستقیمی به هیچ دانشمندی نسبت داده نمی‌شود، حتی اگر درست باشد.

| ❌ | ✅ |
|---|---|
| اینشتین گفت: «خدا تاس نمی‌اندازد.» | یادم می‌آید بزرگی می‌گفت خدا تاس نمی‌اندازد… |

پنج قید: بدون نام · سازگار با علم واقعی · بدون تغییر بار علمی · مرتبط با موضوع · ممنوع در بخش «شاهد تجربی».

**استثنا:** دستاوردها همچنان صریح می‌آیند. «معادلهٔ شرودینگر را شرودینگر در ۱۹۲۶ نوشت» واقعیت مستند است.

### روش کاری وردپرس
پیش از هر تغییر: کدام قالب؟ · موجود است یا باید ساخت؟ · `get_header()`/`get_footer()` هست؟ · CSS در فایل enqueue‌شده؟ · fallback دارد؟ · با افزونهٔ غیرفعال تست شده؟

### زنجیرهٔ تست پیش از ارسال
`php -l` → اجرای واقعی هوک‌ها → بررسی HTML خروجی → اعتبارسنجی DOM.
PHP 8.4 در ورک‌اسپیس نصب است.

---

## ۷ · خطاهایی که رخ داد و رفع شد

| # | خطا | علت | رفع |
|---|---|---|---|
| ۱ | حلقهٔ بی‌نهایت ریدایرکت — ۴ مقاله از دسترس خارج | نگاشت با `basename` کار می‌کرد، مقصد هم شرط را برآورده می‌کرد | شرط مسیر `/glossary/` + بررسی نابرابری مبدأ و مقصد |
| ۲ | ناپدیدشدن ۱۴ دسته | ثبت تاکسونومی در افزونهٔ حذف‌شده بود | انتقال به `functions.php` — داده‌ها سالم بودند |
| ۳ | هاب دو بار رندر | کد دو بار در `functions.php` | حذف بلوک قدیمی، ۵۵۶۱ نویسه |
| ۴ | لوگوی غول‌پیکر | SVG بدون `width`/`height` + CSS ناقص | سه لایه: ابعاد روی تگ، استایل حیاتی، `!important` |
| ۵ | CSS ناقص | فایل جایگزین شده بود نه اضافه | بازسازی کامل ۲۵۴ خط |

**خطای من در سه مورد:** افزونهٔ `Restructure` را در گروه «یک‌بارمصرف» گذاشتم در حالی که ثبت تاکسونومی داشت · ریدایرکت را بدون بررسی حالت مبدأ=مقصد نوشتم · کد را بدون اجرای واقعی PHP فرستادم.

---

## ۸ · تشخیص‌هایی که با آزمون رد شدند

| ادعا | منبع | واقعیت |
|---|---|---|
| «robots.txt خزنده‌ها را بلاک کرده» | تحلیل بیرونی | هر ۵ خزنده HTTP 200 می‌گرفتند |
| «سایت ۵۲۲ می‌دهد» | ابزار دیگر | ۵ بار تست: هر بار HTTP 200 |
| «Qwen را با robots.txt باز کن» | فرض | Qwen اصلاً robots.txt را نمی‌خواند |
| «۱۶۰ مقاله دارید» | سند استراتژی | ۵۹ مقاله + ۲۳ دانشمند |

---

## ۹ · وضعیت فعلی (تأیید زنده)

```
صفحهٔ اصلی          HTTP 200 · ۰٫۲۷s
/qubit/             200      /photon/            200
/topic/fundamentals/ 200     /topic/core-concepts/ 200
/scientists/        200      /glossary/          200

quantum_article  59    quantum_term  0    quantum_scientist  23
```

### ⚠ باز

**۱. نقشهٔ سایت** — `/wp-sitemap.xml` حالا ۳۰۱ به `sitemap_index.xml` می‌دهد که ۴۰۴ است. قانون catch-all در پوسته آدرس نقشه را می‌قاپد. وصله در `qpedia-audit/وصله-نقشه-سایت.md` آماده است.

**۲. هدر و فوتر** — نصب شد ولی لوگو درست ننشست. کد تست‌شده در `AGENT/theme/functions-FINAL.php`.

**۳. لینک شکسته در کارت مقالات** — به `/domain/concepts/` (تاکسونومی قدیمی) می‌رود. با ساخت `single-quantum_article.php` رفع می‌شود.

**۴. `license.txt`** — هنوز ۲۰۰.

---

## ۱۰ · نقشهٔ راه

**فوری:** وصلهٔ نقشهٔ سایت · تکمیل هدر و فوتر

**ساختاری:** `single-quantum_article.php` (کار Article Template را می‌گیرد → ۷ افزونه) · `single-quantum_scientist.php` · `taxonomy-quantum_category.php` · `single.php` (fallback)

**محتوا (متوقف تا پایان ساختار):** ۱۰۱ مقالهٔ باقی‌مانده — یکی در هر نوبت، ورود پنج‌تایی. پنج مقالهٔ اول: کامپیوتر کوانتومی · اصل طرد پاولی · الکترون · معادلهٔ شرودینگر · روز کوانتومی (Q-Day).

---

## ۱۱ · تصاویر شاخص مقالات (Hero Images)

۱۶۰ عکس شاخص با سبک یکسان (مربع · زمینهٔ تیره/سایبری · ترکیب خلوت · نور نئونی) برای موضوعات ۱ تا ۱۶۰ تولید شد. موضوع ۴۴ («صد سال کوانتوم») که در لیست اولیه جا مانده بود اضافه شد.

| مورد | مقدار |
|---|---|
| تعداد کل تصاویر | **۱۶۰ عکس** (موضوع ۱ تا ۱۶۰) |
| فرمت | مربع · PNG |
| قرارداد نام‌گذاری | `images/q_XX_*.png` |
| مسیر در مخزن | `images/` |
| فایل پرامپت‌های انگلیسی | `quantum_image_prompts.md` |
| شاخه | `arena/01a04598-qpedia-ir` |
| درخواست Pull (PR) | https://github.com/lakanzino/qpedia.ir/pull/1 |

نمونهٔ آدرس‌ها (مسیر نسبی در ریپو):
- `images/q_01_quantum.png` … `images/q_43_everett.png`
- `images/q_44_century.png` (صد سال کوانتوم — اضافه‌شده)
- `images/q_45_women.png` … `images/q_160_brain.png`

> همهٔ تصاویر در همان شاخه کامیت شده‌اند و از طریق PRٔ بالا قابل بازبینی/ادغام هستند.

## پیوست · فایل‌های مرجع

| مسیر | محتوا |
|---|---|
| `AGENT/00-دستورالعمل-مرجع.md` | دستورالعمل + بند ۵.۱ |
| `AGENT/ساختار-۱۶۱-با-وضعیت.md` | ۱۶۱ مقاله با وضعیت انتشار |
| `AGENT/ارزیابی-سه-سند.md` | نقد سه سند استراتژی |
| `AGENT/اولویت-اجرا.md` | ترتیب اولویت |
| `AGENT/حذف-افزونه‌ها.md` | طبقه‌بندی افزونه‌ها |
| `AGENT/reference/تحلیل-آموزش-وردپرس.md` | انطباق آموزش با سایت |
| `AGENT/theme/functions-FINAL.php` | کد نهایی تست‌شده |
| `AGENT/theme/qpedia-layouts-FINAL.css` | CSS نهایی |
| `AGENT/test/wp-stub.php` + `run.php` | محیط تست PHP |
| `qpedia-audit/وصله-نقشه-سایت.md` | وصلهٔ نقشه |
