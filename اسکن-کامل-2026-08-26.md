# گزارش اسکن کامل qpedia.ir — ۲۰۲۶-۰۸-۲۶

## دامنهٔ بررسی
این گزارش بر اساس سه منبع تهیه شد:

1. بررسی مستقیم HTTP با `curl` از صفحات اصلی، مقاله، دسته، دانشمندان، واژه‌نامه، robots و sitemap.
2. خزیدن آزمایشی با یک کلاینت دوم برای تشخیص رفتار Cloudflare/WAF.
3. مطالعهٔ کامل PDF پیوست «از تداخل کد تا ظاهر حرفه‌ای: نقشه راه برای ایجاد یک qpedia.ir بهینه‌سازی‌شده»؛ PDF دارای ۳۰ صفحه است.

این گزارش **هیچ تغییری در سایت ایجاد نمی‌کند** و فقط وضعیت فعلی را ثبت می‌کند.

## نتیجهٔ فوری
سایت در نگاه اول Down نیست: صفحات کلیدی با HTTP 200 پاسخ می‌دهند. اما هنوز نمی‌توان آن را از نظر ساختاری «تمیز و تثبیت‌شده» دانست.

سه مشکل واقعی که در اسکن امروز باقی ماند:

- صفحه‌های مقاله هنوز نشانه‌های قالب قدیمی `qpt-*` و پیوندهای قدیمی `/domain/` یا `/glossary/` دارند.
- هدر و فوتر در خروجی چند سیستم/چند لایهٔ تکراری نشان می‌دهند؛ این با گزارش PDF دربارهٔ تداخل سه‌گانه هم‌خوان است.
- نقشهٔ سایت در یک درخواست با `curl` معتبر بود، اما `wp-sitemap.xml` هنوز به `sitemap_index.xml` ریدایرکت می‌شود و robots.txt همچنان آدرس اشتباه `sitemap.xml` را معرفی می‌کند. کل این بخش باید با یک کلاینت ثابت و بعد از پاک‌سازی کش نهایی شود.

## HTTP صفحات کلیدی

| مسیر | وضعیت مشاهده‌شده | اندازهٔ پاسخ | زمان تقریبی |
|---|---:|---:|---:|
| `/` | 200 | 75,046 B | 1.05 تا 3.19 ثانیه |
| `/qubit/` | 200 | 100,827 B | 2.04 ثانیه |
| `/photon/` | 200 | 113,086 B | 2.37 ثانیه |
| `/topic/fundamentals/` | 200 | 72,012 B | 0.89 تا 1.03 ثانیه |
| `/topic/core-concepts/` | 200 | 72,298 B | 1.01 تا 2.13 ثانیه |
| `/scientists/` | 200 | 89,090 B | 1.10 تا 2.20 ثانیه |
| `/glossary/` | 200 | 67,241 B | 0.41 ثانیه |

اعداد زمان متغیرند و به‌دلیل Cloudflare و کش نباید به‌عنوان عدد دائمی سرعت سایت تلقی شوند.

## ساختار خروجی HTML

### صفحهٔ اصلی
- عنوان: `خانه - کوانتوم پدیا فارسی`
- یک `h1`: «دانشنامهٔ کوانتوم»
- یک هاب دسته‌بندی در خروجی پیدا شد؛ در همان اسکن، تعداد `.qp-hub` برابر ۱ بود.
- در شمارش خام HTML، هدر/فوتر پایه و هدر/فوتر سفارشی هم‌زمان قابل مشاهده‌اند؛ این الزاماً به معنی چهار بار نمایش دیداری نیست، اما نشان می‌دهد سیستم هنوز از یک نقطهٔ واحد مدیریت نمی‌شود.

### مقالات
- `/qubit/` و `/photon/` هر دو 200 هستند.
- هر دو نشانهٔ `.qpt` دارند؛ یعنی افزونه/سیستم قدیمی Article Template هنوز در رندر نقش دارد.
- پیوندهای قدیمی در خروجی وجود دارند؛ به‌خصوص پیوندهایی با `/domain/` و `/glossary/`.
- این با تصمیم معماری فعلی مغایر است: دستهٔ اصلی باید `quantum_category` و آدرس‌های `/topic/.../` باشند.

### دسته‌ها
- `/topic/fundamentals/` و `/topic/core-concepts/` 200 هستند.
- عنوان و H1 هر دو درست و فارسیِ قابل‌فهم‌اند.
- این بخش از معماری فعلی برای پایهٔ صفحهٔ اول مناسب است.

### دانشمندان
- `/scientists/` 200 است.
- خروجی تعداد زیادی کارت/بلوک دارد و شمارش خام تگ‌های header/footer به‌علت تکرار اجزای قالب بالا است.
- برای این بخش قالب اختصاصی child theme هنوز لازم است؛ نباید ظاهر آن را به سیستم مقاله وابسته کرد.

### واژه‌نامه
- `/glossary/` از نظر HTTP سالم است، اما عنوان H1 مشاهده‌شده «Nothing Found» بود.
- این مهم است: اگر واژه‌نامه عمداً به ساختار جدید منتقل شده، صفحه باید به یک راهنمای انتقال/فهرست واقعی تبدیل شود؛ اگر قرار است باقی بماند، نباید صفحهٔ خالی با H1 انگلیسی نمایش دهد.

## sitemap و robots

### آنچه امروز دیده شد
با `curl`:

```text
/wp-sitemap.xml  → 301 → /sitemap_index.xml
/sitemap_index.xml → 200 XML
```

محتوای sitemap index شامل این نقشه‌ها بود:

- `quantum_article-sitemap.xml`
- `quantum_scientist-sitemap.xml`
- `quantum_column-sitemap.xml`
- `scientist_field-sitemap.xml`
- `article_domain-sitemap.xml`

اما در درخواست با کلاینت دوم، Cloudflare/WAF پاسخ‌های 403 و برای مسیر زنجیره‌ای 520 داد. بنابراین هنوز باید سلامت sitemap از یک مسیر پایدار و از داخل Search Console یا با تست پس از پاک‌کردن کش تأیید شود.

### ایراد قطعی robots
در robots.txt زنده، خط sitemap این است:

```text
Sitemap: https://qpedia.ir/sitemap.xml
```

در حالی که sitemap واقعیِ مشاهده‌شده `wp-sitemap.xml` است که به Rank Math `sitemap_index.xml` می‌رود. این دوگانگی باید قبل از هر کار جدی SEO اصلاح شود. فعلاً به‌خاطر احتمال دخالت Cloudflare/Rank Math، تغییر کورکورانه پیشنهاد نمی‌شود.

## گزارش PDF چه می‌گوید و کدام بخش آن هنوز معتبر است؟

PDF به‌درستی چهار محور اصلی را تشخیص داده است:

1. تداخل سیستم‌های متعدد هدر/فوتر.
2. تداخل هاب صفحهٔ اول با `loop_start`.
3. باقی‌ماندن taxonomy/post typeهای قدیمی.
4. مشکل sitemap و rewrite.

اما بعضی بخش‌های PDF امروز دیگر وضعیت فعلی را دقیقاً نشان نمی‌دهند:

- PDF می‌گوید sitemap به 404 می‌رود؛ اسکن امروز با `curl` یک Rank Math sitemap با پاسخ 200 دید، هرچند زنجیره و robots هنوز نهایی و قابل‌اعتماد نیست.
- PDF ساختار ۵۹ مقاله و ۲۳ دانشمند را به‌عنوان وضعیت پایه ثبت کرده که با وضعیت ثبت‌شدهٔ پروژه هم‌خوان است.
- پیشنهاد PDF برای یک قالب اختصاصی `single-quantum_article.php`، یک قالب اختصاصی دانشمندان، و حذف منطق layout از plugin همچنان پیشنهاد درست و اولویت‌دار است.
- پیشنهاد PDF برای نگه‌داشتن سه سیستم موازی هدر/فوتر درست نیست؛ باید دقیقاً یک سیستم انتخاب شود و سیستم‌های دیگر واقعاً حذف یا از enqueue خارج شوند.

## نظر فنی دربارهٔ صفحهٔ اول آموزشی/نمایشی

ایدهٔ صفحهٔ اول آموزشی-نمایشی می‌تواند از صفحهٔ فعلی بهتر شود، اما فقط اگر نقش آن روشن باشد. صفحهٔ اول نباید یک انبار کارت و افکت باشد. ترتیب پیشنهادی من:

1. یک معرفی کوتاه و روشن: «کوانتوم را از کجا شروع کنیم؟»
2. یک مسیر آموزشی سه‌مرحله‌ای:
   - شهود و زبان ساده
   - مفاهیم پایه
   - پدیده‌ها و کاربردها
3. یک بخش «شروع سریع» با ۳ یا ۴ مقالهٔ منتخب واقعی، نه فهرست طولانی.
4. هاب دسته‌ها به‌عنوان نقشهٔ راه، نه به‌عنوان محتوای تکراری.
5. بخش آخرین نوشته‌ها با تعداد محدود و عنوان/خلاصهٔ واقعی.
6. یک دعوت مشخص برای ورود به «دانشمندان»، «آزمایش‌ها» و «واژه‌نامه».

از نظر رابط کاربری، نسخهٔ آموزشی باید آرام، خوانا و کم‌تحرک باشد. افکت‌های نئونی و اسکن خطی اگر باقی بمانند باید فقط نقش امضای بصری داشته باشند، نه اینکه توجه را از مسیر یادگیری بگیرند. به‌خصوص در موبایل، اولویت با خوانایی، فاصلهٔ مناسب و دکمه‌های قابل لمس است.

## تصمیم پیشنهادی قبل از ساخت

من فعلاً صفحهٔ جدید را روی سایت نصب نمی‌کنم. ترتیب امن این است:

1. تثبیت یک سیستم واحد header/footer.
2. انتقال ظاهر مقاله به `single-quantum_article.php` و حذف پیوندهای قدیمی.
3. تعیین تکلیف `/glossary/` و H1 «Nothing Found».
4. نهایی‌کردن sitemap و robots.
5. ساخت صفحهٔ اول آموزشی در یک فایل preview مستقل و بررسی دیداری.
6. اجرای `php -l` و تست harness برای PHP؛ سپس تست live روی چند صفحه.
7. فقط بعد از تأیید، جایگزینی صفحهٔ اصلی.

نتیجهٔ من: گزارش PDF از نظر تشخیص ریشهٔ مشکلات ارزشمند است، اما صفحهٔ اول جدید نباید قبل از پاک‌کردن تداخل‌های زیرساختی روی سایت قرار بگیرد. طرح آموزشی ارزش امتحان‌کردن دارد؛ اجرای مستقیم و یک‌بارهٔ آن بدون preview و تست، همان چرخهٔ شکست قبلی را تکرار می‌کند.
