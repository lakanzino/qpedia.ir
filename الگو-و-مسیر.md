# الگوی کاری Qpedia — سند تحویل

> **این فایل را نگه دارید.** اگر ارتباط قطع شد، یا کس دیگری کار را ادامه داد،
> فقط همین فایل کافی است تا مسیر بدون افت کیفیت ادامه پیدا کند.
>
> آخرین بروزرسانی: ۳۱ مرداد ۱۴۰۵ · نسخهٔ ۱.۱

---

## ۰) در یک نگاه: این پروژه چیست؟

`qpedia.ir` — دانشنامهٔ فارسی فیزیک کوانتوم.

**مأموریت:** ساده‌سازی دانش عمومی جهانی کوانتوم به فارسی، با مثال‌های ملموس،
طوری که مخاطب عجیب بودن دنیای کوانتوم را **حس** کند. مخاطب هدف: نسل Z ایران.

**سه موجودیت سایت:**

| موجودیت | نوع پست | آدرس |
|---|---|---|
| مقالات | `quantum_article` | `qpedia.ir/{slug}/` |
| اصطلاحات | `quantum_term` | `qpedia.ir/glossary/{slug}/` |
| دانشمندان | `quantum_scientist` | `qpedia.ir/scientists/{slug}/` |

هدف نهایی: **گراف دانش داخلی** — هر مقاله به اصطلاحات و دانشمندان مرتبط لینک دارد.

---

## ۱) وضعیت فعلی

| مورد | تعداد | یادداشت |
|---|---|---|
| مقالات منتشرشده | ۵ | از ۱۰۰ برنامه‌ریزی‌شده |
| مقالات **نوشته‌شده، آمادهٔ درج** | ۱۰ | ستون ۱ کامل — در `maghalat/html/` |
| اصطلاحات | ۳ | از ۵۹ |
| دانشمندان | ۱۲ | از ۵۲ |

**در حال انجام:** ستون ۱ (مفاهیم بنیادی) ✅ **کامل شد** — ۱۰ مقاله نوشته و تبدیل شده،
منتظر درج در وردپرس. فهرست کامل: `فهرست-تحویل.md`.

**بعدی:** ستون ۲ (پدیده‌های شگفت).

---

## ۲) گردش کار — از ایده تا انتشار

```
۱. انتخاب عنوان از نقشه-مقالات.md
        ↓
۲. نوشتن متن طبق «استاندارد محتوا» (بخش ۳)
        ↓
۳. ذخیره به‌صورت .docx در  maghalat/khaam/
        ↓
۴. اجرای  python3 tabdil.py     → HTML تمیز در maghalat/html/
        ↓
۵. پر کردن meta.json برای آن مقاله
        ↓
۶. اجرای  python3 pishnamayesh.py  → پیش‌نمایش در maghalat/preview/
        ↓
۷. تأیید ظاهر → چسباندن HTML در پیشخوان وردپرس
        ↓
۸. تنظیم فیلدهای سفارشی (بخش ۵)
```

---

## ۳) استاندارد محتوا — قلب پروژه

### ساختار ثابت هر مقاله

| بخش | سبک ورد | نقش |
|---|---|---|
| عنوان | `Title` | یکی، اول سند |
| پاراگراف قلاب | متن معمولی | یک صحنهٔ ملموس، **بدون هیچ اصطلاح فنی** |
| تفکیک شهود | `Heading 1` | «چیزی که فکر می‌کنید» در برابر واقعیت |
| تعریف دقیق | `Heading 1` | حالا اصطلاح را وارد کن |
| شاهد تجربی | `Heading 1` | کدام آزمایش، چه سالی، چه کسی |
| سوءتفاهم رایج | `Heading 1` | حیاتی برای مخاطب فارسی‌زبان |
| کاربرد | `Heading 1` | چه چیزی در جیبت بر این استوار است |
| پرسش‌های پرتکرار | `Heading 1` + چند `Heading 2` | برای People Also Ask گوگل |
| جمع‌بندی | `Heading 1` | دو پاراگراف، بدون تکرار |

### قاعدهٔ طلایی

> هر مقاله باید یک لحظهٔ **«صبر کن، چی؟!»** داشته باشد.
> اگر خواننده تا پایان یک‌بار هم جا نخورد، مقاله شکست خورده است.

### لحن

| بکن | نکن |
|---|---|
| دوم‌شخص: «تصور کنید»، «فرض کنید» | «همان‌طور که می‌دانیم» |
| جملهٔ کوتاه؛ یک ایده در هر پاراگراف | پاراگراف ده‌خطی |
| قیاس ملموس **پیش از** فرمول | فرمول بدون زمینه |
| «هنوز نمی‌دانیم» وقتی واقعاً نمی‌دانیم | قطعیت جعلی |
| نام فیزیک‌دان + سال دقیق | «دانشمندان می‌گویند» |
| رد صریح ادعاهای شبه‌علمی | سکوت مؤدبانه در برابرشان |

### سه خط قرمز

**۱. متن خام هوش مصنوعی منتشر نشود.** گوگل از مارس ۲۰۲۴ روی محتوای انبوه ماشینی
سخت‌گیر است. حداقل کار: قلاب ابتدایی را خودتان بنویسید و یک مثال بومی داخل متن بگذارید.

**۲. E-E-A-T.** پایین هر مقالهٔ علمی دو یا سه مرجع واقعی. ویکی‌پدیا کافی نیست —
به مقالهٔ اصلی، کتاب درسی، یا صفحهٔ نوبل ارجاع دهید.

**۳. ترتیب انتشار.** ستون به ستون. اگر پراکنده منتشر کنید،
لینک‌های داخلی به مقالات منتشرنشده می‌خورند و گراف دانش شکل نمی‌گیرد.

### سئو

- طول: **۱۲۰۰ تا ۱۸۰۰ کلمه**
- اسلاگ: لاتین، کوتاه، بدون stop word
- عنوان: زیر ۶۰ کاراکتر
- توضیح متا: ۱۲۰ تا ۱۵۵ کاراکتر
- **در بدنه `<h1>` ننویسید** — وردپرس خودش می‌سازد
- ۳ تا ۶ لینک داخلی در هر مقاله
- بدون تصویر استوک؛ یا SVG متحرک یا هیچ

---

## ۴) فایل‌های پروژه

```
/home/user/maghalat/
├── khaam/              ← فایل‌های ورد ورودی (اینجا آپلود کنید)
├── html/               ← خروجی HTML تمیز
├── preview/            ← پیش‌نمایش آفلاین قالب
├── tabdil.py           ← ورد → HTML
├── pishnamayesh.py     ← ساخت پیش‌نمایش
├── meta.json           ← اطلاعات تکمیلی هر مقاله
├── نقشه-مقالات.md      ← فهرست ۱۰۰ مقاله با اسلاگ و قلاب
├── فهرست-تحویل.md      ← جدول درج ستون ۱ در وردپرس
├── راهنما.md           ← راهنمای نوشتن سند ورد
└── الگو-و-مسیر.md      ← همین فایل

/home/user/qpedia-audit/
├── qpedia-tpl.php.txt  ← افزونهٔ قالب مقالات
├── qpedia-shell.php.txt← هدر و فوتر سایت (نصب‌شده)
├── qpedia-arch.php.txt ← ساختار سه‌موجودیتی (نصب‌شده)
├── changelog.txt       ← تاریخچهٔ کامل کارها — نسخهٔ ۱.۳.۰
└── بخش-رفع-اصطلاحات.txt← وصلهٔ نصب‌نشده (بخش ۷)
```

### دستورها

```bash
cd /home/user/maghalat
python3 tabdil.py                    # همهٔ فایل‌های khaam را تبدیل کن
python3 pishnamayesh.py              # پیش‌نمایش همه
python3 pishnamayesh.py نام-فایل     # پیش‌نمایش یکی
```

---

## ۵) فیلدهای سفارشی وردپرس

بعد از چسباندن محتوا، در ویرایشگر پست این‌ها را پر کنید:

| کلید | مثال | اثر |
|---|---|---|
| `qpt_en` | `QUANTUM SUPERPOSITION` | عنوان انگلیسی زیر تیتر کارت |
| `qpt_lead` | یک پاراگراف | جایگزین چکیدهٔ خودکار |
| `qpt_nokat` | هر خط یک نکته | جعبهٔ سبز «نکات کلیدی» |
| `qpt_terms` | `photon, laser` | چیپ اصطلاحات مرتبط (اسلاگ لاتین) |
| `qpt_scientists` | `albert-einstein` | چیپ دانشمندان مرتبط |
| `qpt_off` | `1` | این مقاله را از قالب مستثنا کن |

> فیلدهای سفارشی دیده نمی‌شوند؟ سه‌نقطهٔ بالا-راست ویرایشگر ←
> **ترجیحات** ← **پنل‌ها** ← **فیلدهای سفارشی** را روشن کنید.

**فراموش نکنید:** ستون (`article_pillar`) و حوزه (`article_domain`) هر مقاله را تنظیم کنید،
وگرنه بخش «مقالات مرتبط» خالی می‌ماند.

---

## ۶) نصب افزونهٔ قالب

فایل `qpedia-tpl.php.txt` را به `qpedia-tpl.php` تغییر نام دهید و در
`/wp-content/plugins/qpedia-tpl/` بگذارید. سپس **LiteSpeed ← Purge All**.

جایگزین: محتوایش را (بدون `<?php` اول) به انتهای `qpedia-shell.php` بچسبانید.

---

## ۷) کارهای باز

| اولویت | کار |
|---|---|
| 🔴 | نصب `بخش-رفع-اصطلاحات.txt` — رفع باگ «خانه»، ۴۰۴ دسته‌های واژه‌نامه، `og:image` |
| 🔴 | بعد از نصب: تنظیمات ← پیوندهای یکتا ← **ذخیره** (فلاش قوانین) |
| 🟠 | نوشتن ۹۵ مقالهٔ باقی‌مانده |
| 🟠 | اجرای وارد‌کنندهٔ ۵۲ دانشمند (۱۲ انجام شده) |
| 🟠 | اجرای وارد‌کنندهٔ ۵۹ اصطلاح (۰ انجام شده) |
| 🟡 | آپلود `max-planck.jpg` و `hideki-yukawa.jpg` |
| 🟡 | تبدیل اسلاگ فارسی `منحنی-لیساژو` به لاتین |
| 🟡 | کوتاه کردن عنوان ۱۲۹ کاراکتری صفحهٔ اصلی |
| 🟡 | حذف فایل فیزیکی `robots.txt` |
| ⚪ | نقشهٔ سایت — **معطل** تا ثبت در Search Console |

---

## ۸) اگر ادامه‌دهنده هستید

**در پیام اول این را بگویید:**

> فایل `/home/user/maghalat/الگو-و-مسیر.md` را بخوان.
> می‌خواهیم مقالات ستون [شماره] از `نقشه-مقالات.md` را ادامه دهیم.
> نمونهٔ تأییدشدهٔ سبک: `maghalat/html/برهم‌نهی-کوانتومی.html`

**سه چیزی که نباید عوض شود:**

۱. **ساختار هشت‌بخشی** مقالات (بخش ۳)
۲. **تم دولایه** — سایت پس‌زمینهٔ روشن دارد (`#f4f7fc`). جعبه‌های تأکیدی
   (کارت معرفی، نکات کلیدی) تیره و نئونی‌اند؛ متن بدنه تیره روی روشن.
   نئون `#00e676` فقط روی پس‌زمینهٔ تیره؛ روی روشن از `#15734a` استفاده کنید.
   همهٔ ترکیب‌ها باید کنتراست ≥ ۴٫۵ داشته باشند (استاندارد WCAG AA).
۳. **قالب در زمان نمایش** — هرگز HTML قالب را داخل محتوای پست نپزید

**سه چیزی که آزاد است:**

۱. ترتیب مقالات داخل هر ستون
۲. انتخاب قلاب ملموس (فقط باید واقعاً ملموس باشد)
۳. تعداد و عنوان پرسش‌های پرتکرار

---

## ۹) چرا این تصمیم‌ها گرفته شد؟

**چرا قالب در زمان نمایش، نه داخل محتوا؟**
با ۱۰۰ مقاله، تغییر یک رنگ نباید یعنی ویرایش ۱۰۰ پست. الان یک فایل عوض می‌شود
و همه بروز می‌شوند. محتوای دیتابیس هم تمیز و قابل انتقال می‌ماند.

**چرا اصطلاحات تصویر شاخص ندارند؟**
چون داخل بدنه‌شان SVG متحرک هست. تصویر شاخص تکراری می‌شود.
تنها پیامدش `og:image` است که با تصویر پیش‌فرض سایت حل می‌شود.

**چرا آدرس مقالات کوتاه است (`qpedia.ir/slug/`)؟**
تصمیم صریح مالک سایت. بدون بخش `/category/` در هیچ آدرس عمومی.

**چرا مثال ملموس این‌قدر تأکید شده؟**
چون هدف پروژه انتقال **حسِ** عجیب بودن است، نه انتقال اطلاعات.
اطلاعات در ویکی‌پدیا هست؛ چیزی که نیست، لحظهٔ «صبر کن، چی؟!» است.
