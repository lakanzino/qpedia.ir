# ترتیب اولویت تغییرات + تصمیم افزونه‌ها

---

## بخش ۱ — افزونه‌ها: موافقم، اما نه با همه

### قاعده‌ای که تصمیم را روشن می‌کند

سه نوع کد داریم و هرکدام جای متفاوتی دارند:

| نوع کد | کجا باید باشد | چرا |
|---|---|---|
| **ابزار موقت** (ایمپورت مقاله) | افزونه — بعد حذف | کارش که تمام شد، وزنش را از سایت بردارید |
| **ظاهر و چیدمان** (هدر، فوتر، استایل، منو) | **قالب فرزند ✅** | جزء هویت بصری سایت است؛ اگر قالب عوض شود باید با آن برود |
| **رفتار و داده** (CPT، تاکسونومی، ریرایت، وصلهٔ نقشهٔ سایت) | **افزونهٔ دائمی ⚠** | باید از تعویض قالب **جان سالم به در ببرد** |

### پس نظر شما دربارهٔ هدر درست است

هرچه مربوط به **ظاهر** است — تغییرات هدر، فوتر، استایل، اسکریپت‌های نمایشی — باید در `functions.php` قالب فرزند باشد. اینها به قالب گره خورده‌اند و جای دیگری معنا ندارند.

### اما یک استثنای مهم

**`qpedia-fixes.php` را به قالب منتقل نکنید.**

آن افزونه ثبت پست‌تایپ‌ها (`quantum_article`، `quantum_term`، `quantum_scientist`)، تاکسونومی‌ها و وصلهٔ نقشهٔ سایت را نگه می‌دارد. اگر اینها به `functions.php` بروند، روزی که قالب را عوض کنید یا به‌روزرسانی قالب فرزند را خراب کند، **۸۷ محتوای شما ناپدید می‌شود**. نه پاک می‌شود — در دیتابیس می‌ماند ولی وردپرس دیگر نمی‌داند چطور نمایششان دهد.

این قاعدهٔ رسمی وردپرس است و استثنا ندارد: **داده در افزونه، ظاهر در قالب.**

### نقشهٔ عملی

```
قالب فرزند quantum-pedia-child/functions.php
├── تغییرات هدر
├── تغییرات فوتر
├── استایل‌ها و اسکریپت‌های نمایشی
└── قالب‌بندی محتوا

افزونهٔ دائمی qpedia-fixes.php  ← دست نزنید
├── ثبت CPTها
├── ثبت تاکسونومی‌ها
├── قوانین ریرایت
└── وصلهٔ نقشهٔ سایت

افزونه‌های موقت  ← بعد از هر ایمپورت غیرفعال و حذف
└── ابزار ایمپورت مقاله
```

### هشدار پیش از جابه‌جایی

۱. **پشتیبان کامل** بگیرید (فایل + دیتابیس) — قبل از دست‌زدن به `functions.php`
۲. **یکی‌یکی** منتقل کنید، نه همه با هم. بعد از هر انتقال سایت را ببینید
۳. یک خطای نحوی در `functions.php` = **صفحهٔ سفید کل سایت**. برخلاف افزونه که می‌شود از پنل غیرفعالش کرد، قالب خراب یعنی دسترسی به پنل هم قطع می‌شود
۴. راه نجات را از قبل بدانید: با File Manager هاست، نام پوشهٔ قالب فرزند را عوض کنید تا وردپرس به قالب مادر برگردد

---

## بخش ۲ — ترتیب اولویت تغییرات

مرتب‌شده بر اساس **«اگر این نباشد، کارهای بعدی بی‌فایده‌اند»** — نه بر اساس سختی.

### 🔴 اولویت ۰ — پیش از هر چیز

**۱. وصلهٔ نقشهٔ سایت** (`/home/user/qpedia-audit/وصله-نقشه-سایت.md`)

نقشه ساخته می‌شود، محتوایش کامل است، ولی با کد ۴۰۴ تحویل داده می‌شود. هر خزنده‌ای با دیدن ۴۰۴ دورش می‌اندازد.

> تا این انجام نشود، هر کار سئویی روی سایتی انجام می‌شود که موتور جستجو فهرست صفحاتش را نمی‌بیند. این تنها موردی است که بقیه به آن وابسته‌اند.

**۲. اصلاح آدرس نقشه در robots.txt** — الان `sitemap.xml` نوشته‌اید که وجود ندارد. درستش `wp-sitemap.xml` است.

### 🟠 اولویت ۱ — پاکسازی (بدون ریسک)

۳. حذف ۱۱ دستهٔ خالی `categories` + ۲۳ تگ خالی — ۳۴ صفحهٔ بی‌محتوا
۴. حذف `license.txt` از ریشهٔ هاست
۵. اصلاح اسلاگ فارسی «منحنی لیساژو» → `lissajous-curve` **با ریدایرکت ۳۰۱**
۶. حذف دستهٔ `ntanglement` (غلط تایپی، در `tax_categories.json` دیده شد)

### 🟡 اولویت ۲ — ساختار

۷. ساخت تاکسونومی واحد با ۷ دسته / ۱۱ زیردسته
۸. انتساب ۴۰ مقالهٔ موجود — **بدون تغییر هیچ URL**
۹. حذف `article_domain` (تناقض‌های ثابت‌شده دارد)

### 🟢 اولویت ۳ — انتقال کدها

۱۰. جابه‌جایی کدهای ظاهری به قالب فرزند (بخش ۱)

### 🔵 اولویت ۴ — تولید محتوا

۱۱. **۱۰۵ مقالهٔ نو** + **۱۵ ارتقای اصطلاح** = ۱۲۰ واحد کار
۱۲. Pillar — فقط برای دسته‌ای با حداقل ۱۰ مقالهٔ منتشرشده

---

## بخش ۳ — روال تولید مقاله

**قاعدهٔ شما، پذیرفته:** هر نوبت **یک** مقاله، بی‌خطا، با رعایت کامل قواعد. هر ۵ تا که شد → ایمپورت.

### چک‌لیست هر مقاله (پیش از تحویل)

- [ ] ساختار: قلاب → تفکیک شهود → تعریف دقیق → شاهد تجربی → سوءتفاهم رایج
- [ ] عنوان طبق فرمول `[اصطلاح] چیست؟`
- [ ] اسلاگ لاتین، کوتاه، بدون کاراکتر فارسی
- [ ] حداقل ۳ لینک داخلی به مقالات موجود — **فقط به اسلاگ‌های تأییدشدهٔ منتشرشده**
- [ ] دستهٔ درست طبق `ساختار-۱۶۱-با-وضعیت.md`
- [ ] بدون ادعای علمی نادرست
- [ ] `meta.json` هماهنگ با فیلدهای داشبورد

### ترتیب پیشنهادی پنج مقالهٔ اول

انتخاب بر دو معیار: **پرجستجو بودن** و **تکمیل شکاف در دسته‌های نیمه‌پر**.

| # | مقاله | دسته | چرا اول |
|---|---|---|---|
| ۱ | **۱۱۷ کامپیوتر کوانتومی چیست؟** | quantum-computing | پرجستجوترین عبارت کل حوزه در فارسی · دسته‌ای که فقط ۲ از ۲۳ مقاله‌اش آماده است |
| ۲ | **۱۵ اصل طرد پاولی** | core-concepts | تنها حفرهٔ «مفاهیم پایه» — با آن، دسته ۱۵/۱۵ کامل می‌شود |
| ۳ | **۱۷ الکترون** | particles | دسته‌ای با فقط ۲ از ۱۰ · مفهوم پایه که همه‌چیز به آن لینک می‌دهد |
| ۴ | **۹۱ معادلهٔ شرودینگر** | mathematics | ستون فقرات ریاضیات · جستجوی آموزشی بالا |
| ۵ | **۱۶۱ روز کوانتومی (Q-Day)** | quantum-computing | 🆕 اجماع هر سه سند · رقابت فارسی تقریباً صفر |

> پس از این پنج‌تا: «مفاهیم پایه» کامل می‌شود و «رایانش کوانتومی» از ۲ به ۴ می‌رسد.
