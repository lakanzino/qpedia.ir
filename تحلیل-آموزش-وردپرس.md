# تحلیل آموزش وردپرس — و انطباقش با وضعیت واقعی qpedia

> سند اصلی: `AGENT/reference/wordpress-agent-training-guide-1.md`
> تشخیص با تست زنده انجام شد، نه با فرض.

---

## بخش ۱ — تشخیص: سایت شما «لخت» نیست

بخش ۸ سند یک چک‌لیست چهارمرحله‌ای برای «سایت لخت شد» دارد. اجرایش کردم روی `/qubit/`:

```
body class : single-quantum_article · wp-theme-quantum-pedia ✅
<header>   : ۲ عدد ✅
<footer>   : ۲ عدد ✅
site-header · site-footer · site-main · entry-content : همه حاضر ✅
شورت‌کد مردهٔ افزونه : هیچ ✅
CSS پوستهٔ فرزند : لود می‌شود ✅
```

**نتیجه: مشکل «سایت لخت» شما وجود ندارد.**

`get_header()` و `get_footer()` صدا زده می‌شوند. آنچه از دست رفت، طراحی اختصاصی Shell بود — نه ساختار پوسته. سایت الان **پوستهٔ پایه** را نشان می‌دهد که سالم ولی ساده است.

**پس بخش‌های ۴ و ۸ سند (رفع فوری) برای شما موضوعیت ندارد.** این مهم است چون اگر طبق آن پیش می‌رفتیم، دنبال مشکلی می‌گشتیم که نیست.

### اما یک چیز واقعاً غایب است

```
quantum_article-template-default
```

یعنی وردپرس **قالب پیش‌فرض** را استفاده می‌کند. هیچ `single-quantum_article.php` در پوستهٔ فرزند وجود ندارد. مقاله، پروفایل دانشمند و برگه همه یک شکل نمایش داده می‌شوند.

**این دقیقاً همان چیزی است که بخش ۲ و ۵ سند حل می‌کند — و برای شما مفید است.**

---

## بخش ۲ — چه چیزی از سند به کار ما می‌آید

| بخش سند | ارزیابی | چرا |
|---|---|---|
| **۱ جدایی محتوا از طراحی** | ✅ **بپذیر** | ریشهٔ همان اتفاقی است که با حذف Shell افتاد |
| **۲ سلسله‌مراتب قالب** | ✅ **بپذیر — کلید کار** | با `single-quantum_article.php` هر نوع محتوا ظاهر خودش را می‌گیرد، بدون افزونه |
| **۳ ساختار پوستهٔ فرزند** | ✅ **بپذیر** | شما `assets/css/custom.css` دارید؛ ساختار درست است |
| **۴ همیشه header/footer** | ➖ **موضوعیت ندارد** | تست شد، هست |
| **۵ الگوی چیدمان ثابت** | ⚠ **نیمه‌بپذیر** | ایدهٔ درست، ولی بخش ACF برای شما اشتباه است — پایین توضیح |
| **۶ ستون‌بندی بدون page builder** | ✅ **بپذیر** | CSS Grid، بدون وابستگی |
| **۷ شبکهٔ ایمنی fallback** | ✅ **بپذیر** | `single.php` عمومی — بیمهٔ ارزان |
| **۸ تشخیص سایت لخت** | ➖ **موضوعیت ندارد** | اجرا شد، سایت سالم است |
| **۹ عملکرد و امنیت** | ⚠ **یک تناقض دارد** | پایین |
| **۱۰ قواعد طلایی** | ✅ **بپذیر** | همه درست |

---

## بخش ۳ — سه جا که با سند مخالفم

### ❌ ۱. ACF برای شما اشتباه است

سند می‌گوید فیلدهای ACF تعریف کنید و نویسنده فقط فیلد پر کند.

**چرا برای qpedia کار نمی‌کند:** محتوای شما **مقالهٔ روایی** است، نه دادهٔ ساختاریافته. هر مقاله ۴۰۰۰–۵۹۰۰ نویسه با ساختار «قلاب → تفکیک شهود → تعریف دقیق → شاهد تجربی → سوءتفاهم رایج» دارد. این در فیلد نمی‌گنجد.

ACF برای پروفایل دانشمند منطقی بود (سال تولد، حوزه، عکس) — ولی ۲۳ پروفایل شما **از قبل نوشته شده‌اند** و تبدیلشان به فیلد یعنی بازنویسی همه.

> **مهم‌تر:** سند خودش می‌گوید ACF یک **افزونه** است. شما همین الان از ۱۶ افزونه به ۸ رسیدید و صریحاً گفتید افزونهٔ بیشتر نمی‌خواهید. اضافه‌کردن ACF خلاف جهت کاری است که کردیم.

**جایگزین:** ساختار در **قالب** باشد، محتوا در `post_content` معمولی. همان کاری که تا الان کرده‌اید و درست است.

### ⚠ ۲. تناقض داخلی سند دربارهٔ CSS

بخش ۹ می‌گوید: «هرگز `<style>` درون‌خطی در قالب استفاده نکن — همیشه `wp_enqueue_style()`.»

**درست است و می‌پذیرم** — ولی توجه: کدی که قبلاً برای `functions.php` دادم دقیقاً از `<style>` درون‌خطی استفاده می‌کرد. آن راه سریع بود، این راه درست است.

**اصلاح:** CSS به `assets/css/custom.css` منتقل شود که از قبل در پوستهٔ فرزند شما وجود دارد و لود می‌شود.

### ⚠ ۳. «هرگز page builder» — با احتیاط

اصل درست است، ولی نتیجه‌اش این نیست که هیچ افزونه‌ای نباید ظاهر بسازد. تفاوت مهم:

| نوع | سرنوشت پس از حذف |
|---|---|
| **Page builder** (شورت‌کد در محتوا) | محتوا **خراب** می‌شود — متن با `[vc_row]` پر می‌ماند |
| **افزونهٔ قالب‌ساز** (مثل Shell شما) | فقط ظاهر می‌رود، **محتوا سالم می‌ماند** |

Shell از نوع دوم بود — به همین دلیل با حذفش هیچ محتوایی خراب نشد. سند این تمایز را نمی‌گذارد.

---

## بخش ۴ — نقشهٔ عملی برای qpedia

بر اساس آنچه پذیرفتیم، و **بدون هیچ افزونهٔ جدید**:

```
quantum-pedia-child/
├── style.css                      ← موجود
├── functions.php                  ← موجود، افزودنی دارد
├── assets/css/custom.css          ← موجود، CSS جدید اینجا
├── single-quantum_article.php     🆕 قالب مقاله
├── single-quantum_scientist.php   🆕 قالب دانشمند
├── archive-quantum_article.php    🆕 آرشیو
├── taxonomy-quantum_category.php  🆕 آرشیو دسته
└── single.php                     🆕 fallback ایمنی
```

**پنج فایل جدید. صفر افزونه.**

### چرا این بار خراب نمی‌شود

| سناریو | نتیجه |
|---|---|
| افزونه‌ای حذف شود | ظاهر دست‌نخورده ✅ |
| پوستهٔ مادر به‌روز شود | فرزند دست‌نخورده ✅ |
| نوع محتوای جدید اضافه شود | `single.php` می‌گیردش ✅ |
| قالب اشتباه انتخاب شود | سلسله‌مراتب خودکار درستش می‌کند ✅ |

تنها راه خراب‌شدن: کسی فایل‌های پوستهٔ فرزند را پاک کند.

---

## بخش ۵ — روش کاری جدید (طبق درخواست شما)

از این پس، پیش از هر کار وردپرسی این چک‌لیست اجرا می‌شود:

- [ ] کدام نوع محتوا و کدام فایل قالب درگیر است؟
- [ ] آیا قالب اختصاصی هست یا باید ساخته شود؟
- [ ] `get_header()` و `get_footer()` حاضرند؟
- [ ] CSS در فایل enqueue‌شده است، نه درون‌خطی و نه وابسته به افزونه؟
- [ ] fallback وجود دارد؟
- [ ] با غیرفعال‌کردن افزونه‌ها تست شده؟

و شش قاعدهٔ ثابت:

۱. طراحی در **پوسته**، هرگز فقط در افزونه
۲. یک فایل قالب برای هر نوع محتوا
۳. هر قالب `get_header()` و `get_footer()` دارد
۴. همیشه یک fallback عمومی
۵. CSS با کلاس اختصاصی هر نوع محتوا (`.qp-article-*`, `.qp-scientist-*`)
۶. هر خروجی پویا escape شود: `esc_html()`, `esc_url()`, `esc_attr()`

---

## قدم بعدی

پنج فایل قالب را می‌سازم — هر کدام با دکمهٔ کپی، کامنت‌گذاری‌شده، و آمادهٔ چسباندن.

**یک تصمیم لازم است:** طراحی مقالات چه باشد؟

**الف)** ساده و خوانا — تک‌ستون، تایپوگرافی تمیز، فهرست مطالب کنار
**ب)** نزدیک به ظاهر قبلی Shell — هدر تیره، کارت‌بندی، رنگ فیروزه‌ای
**ج)** اول یکی را بسازم، ببینید، بعد بقیه
