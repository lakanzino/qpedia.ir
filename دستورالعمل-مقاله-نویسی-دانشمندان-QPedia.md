# دستورالعمل مقاله‌نویسی دانشمندان QPedia — نسخهٔ ۱٫۱

> سند اختصاصیِ نوشتن مدخل‌های «دانشمندان کوانتوم» (post_type: `quantum_scientist`).
> این سند «زنده» است و از این پس مرجعِ الزام‌آور برای هر مدخلِ دانشمند است — نه دستورالعمل مقالهٔ آموزشی.
> تاریخ تصویب: ۳ شهریور ۱۴۰۵ · مکملِ `دستورالعمل-مقاله-نویسی-QPedia.md` و تابعِ `قوانین-الزامی.md`.

---

## بخش ۰ — چرا دانشمند سندِ جدای خودش را دارد؟

مقالهٔ آموزشی (`quantum_article`) یک **مفهوم** را توضیح می‌دهد؛ مدخلِ دانشمند (`quantum_scientist`) یک **آدم** را روایت می‌کند. این دو ژانر متفاوت‌اند:

| مقالهٔ آموزشی | مدخلِ دانشمند |
|---|---|
| H2 پرسشی و ساختارمند | H2 روایی و قصه‌گونه |
| دقت علمی بالا، قیاس قبل از فرمول | روایت با هیجان و آب‌وتاب؛ دقت علمی در حدِ «درست بودن داستان»، نه فرمول |
| نقل‌قول مستقیم ممنوع (مگر همان‌جلسه دیده شود) | نقل‌قول مجاز است، اما **فقط تأییدشده** (قانون ۱ قوانین الزامی همچنان حاکم است) |
| موضوع: مفهوم | موضوع: زندگی، کار، خانواده، همکاران، خوشی‌ها و بدبختی‌ها |

**اصل تفاوت (سخن مالک):** اینجا «آزادی گفتار» داریم؛ روایت‌هایی که نقل شده‌اند را با هیجان و آب‌وتابِ جذاب روایت می‌کنیم. **اما آزادیِ روایت ≠ آزادیِ جعل.** ما می‌توانیم یک رویدادِ واقعی را زیبا و دراماتیک تعریف کنیم؛ نمی‌توانیم رویدادی بسازیم که وجود نداشته. خطِ قرمزِ صداقت (قوانین الزامی) هیچ‌جا برداشته نمی‌شود.

---

## بخش ۱ — قالبِ فنیِ زنده (آنچه سرور قبلاً می‌سازد)

این‌ها از فایل‌های زندهٔ سایت استخراج شده‌اند و نباید در متنِ مقاله بازنویسی شوند؛ فقط رعایت می‌شوند:

### ۱) نوعِ محتوا و نشانی
- `post_type`: `quantum_scientist`
- نشانی: `https://qpedia.ir/scientists/<slug>/`
- آرشیو: `https://qpedia.ir/scientists/`
- پشتیبانی فیلدها: `title`, `editor`, `excerpt`, `thumbnail`, `author`

### ۲) آنچه قالبِ تک‌مدخلی خودش رندر می‌کند
قالب `single-quantum_scientist` به‌ترتیب این‌ها را می‌سازد — پس **هیچ‌کدام داخل بدنه نوشته نمی‌شود**:
1. برچسب بالای صفحه: «دانشمند کوانتوم» (eyebrow)
2. چیپس‌های دسته‌بندی از دو تاکسونومی: `scientist_field` (زمینهٔ علمی) و `quantum_scientist_group` (گروه/دوره)
3. `H1` = عنوان مدخل (وردپرس از فیلد «عنوان» می‌سازد)
4. نام لاتین: متای `_scientist_en_name`
5. متا: تاریخ انتشار + «بازگشت به فهرست دانشمندان»
6. چکیده (excerpt)
7. تصویر شاخص (thumbnail)
8. بدنه (شورت‌کد مردهٔ `[qsci_card]` به‌صورت خودکار حذف می‌شود)

### ۳) تفاوت فنی با قالب مقاله
- قالب دانشمند **فهرست مطالب (TOC) ندارد**، بخش «مقاله‌های مرتبط» ندارد، و ناوبری قبلی/بعدی ندارد.
- پس TOC را نه در متن می‌سازیم و نه انتظارش را داریم؛ روانیِ روایت، جای TOC را می‌گیرد.
- تاکسونومی `scientist_field` از نقشهٔ سایت (sitemap) حذف شده؛ لینک‌دهیِ کشف‌پذیری باید از مسیر لینک داخلی و آرشیو بیاید.

### ۴) نتیجهٔ عملی برای نویسنده
نویسنده فقط این‌ها را تحویل می‌دهد (جدا از متن، مثل مقاله):
- **عنوان** (فارسی، نام دانشمند + توصیف کوتاه درخشان)
- **نام لاتین** → متای `_scientist_en_name`
- **اسلاگ** (لاتین کوتاه)
- **چکیده/excerpt** (۲ جمله)
- **دسته‌ها**: `scientist_field` + `quantum_scientist_group`
- **بدنه** (HTML ساده — بدون H1)

---

## بخش ۲ — قالبِ نوشتاریِ استخراج‌شده از نمونه‌های موجود

از مدخل‌های منتشرشده (پلانک، بور، بل، دیراک، شرودینگر، فاینمن، …) این الگو بیرون آمده و «همین» را ادامه می‌دهیم:

1. **لیدِ ضربه‌ای (قلاب):** ۱ تا ۳ پاراگراف کوتاه که با یک تناقض، یک تصویر، یا یک جملهٔ غافلگیرکننده شروع می‌شود.
   - نمونهٔ زندهٔ پلانک: «ماکس پلانک کوانتوم را کشف کرد و بعد سال‌ها امیدوار بود کسی نشان دهد اشتباه کرده است.»
   - نمونهٔ زندهٔ بور: «او گفت الکترون دور هسته می‌چرخد اما انرژی از دست نمی‌دهد… بور توضیحی نداد. فقط گفت: این‌طور است، چون اتم‌ها فرو نمی‌ریزند. و درست می‌گفت.»
2. **بدنهٔ روایی (H2):** فصل‌های قصه، نه پرسش‌های خشک. هر H2 یک پرده از زندگی است، نه یک جوابِ دایره‌المعارفی.
3. **نقل‌قول (Blockquote):** جمله‌های درخشانِ تأییدشدهٔ خودِ دانشمند یا هم‌عصرانش.
4. **فهرست (ul/ol):** برای خطِ زمانی، برای فاجعه‌های پشت‌سرهم، برای فهرست همکاران.
   - نمونهٔ پلانک: فهرستِ فقدان‌ها (همسر، پسر در وردن، دو دختر در زایمان، بمباران خانه، اعدام پسر آخر).
5. **پرسش‌های پرتکرار (H2 + H3):** ۳ تا ۵ پرسش واقعیِ خواننده با پاسخ کوتاه.
6. **میراث (H2):** جمع‌بندیِ احساسی/فکری — «این آدم در نهایت چه بر جای گذاشت».
7. **منابع (H2):** ۱ تا ۲ منبع معتبر (این بخش را برای گوگل **اضافه می‌کنیم** — نمونه‌های قدیمی آن را ندارند؛ بخش ۶).

> نکته: نمونه‌های فعلی «منابع» ندارند و پایانشان «میراث» است. در این سند، «منابع» به‌عنوان بخش پایانی اضافه می‌شود (الزامِ گوگل برای اعتبار). ترتیب پایانی جدید: **میراث ← پرسش‌های پرتکرار ← منابع** (منابع همیشه آخرین بخش).

---

## بخش ۳ — اقلام مهمِ منتقل‌شده از دستورالعمل مقاله (اینجا → آن‌جا)

این‌ها قواعد سئو/ساختاریِ مقالهٔ آموزشی است که **عیناً** در مدخلِ دانشمند هم اجرا می‌شود:

| # | قاعده | اجرا در دانشمند |
|---|---|---|
| ۱ | **بدنه بدون H1** | وردپرس H1 را از عنوان می‌سازد؛ بدنه از H2 شروع می‌شود |
| ۲ | **عنوان زیر ۶۰ کاراکتر** | فارسی، حاوی نام دانشمند؛ متمایل به کوتاه |
| ۳ | **اسلاگ لاتین کوتاه** | ۱ تا ۲ کلمه، بدون stop word (مثل `max-planck`، `john-bell`) |
| ۴ | **چکیده/متا ۱۲۰–۱۵۵ کاراکتر** | ۲ جمله: جملهٔ اول قلاب، جملهٔ دوم پوشش |
| ۵ | **HTML سادهٔ آماده** | فقط تگ‌های مجاز (h2 h3 p strong em blockquote ul ol li a details summary)؛ ممنوع: div/span/style/class |
| ۶ | **لینک داخلی ≥۵** | به مقاله‌های آموزشی (`/qubit/`) **و** به مدخل‌های دیگر دانشمندان (`/scientists/.../`) |
| ۷ | **بولدِ نخستین ظهور** | اولین ظهور هر نام/اصطلاح کلیدی بولد |
| ۸ | **سئو جدا از متن** | عنوان/اسلاگ/چکیده/دسته‌ها جدا نگه‌داری می‌شود تا با ایمپورتر وارد شود |
| ۹ | **نیم‌فاصلهٔ صحیح** | با تحویل فایل + ایمپورتر، نیم‌فاصله (ZWNJ) سالم می‌ماند؛ متن با نیم‌فاصلهٔ درست نوشته می‌شود |
| ۱۰ | **پیش‌فرض پیش‌نویس** | هرگز مستقیم منتشر نمی‌شود |

---

## بخش ۴ — تفاوت‌ها: آزادیِ روایت

اینجا آنچه در مقالهٔ آموزشی «خط قرمز» است، در مدخلِ دانشمند «امکان» است — مشروط به صداقت:

| مورد | مقالهٔ آموزشی | مدخلِ دانشمند |
|---|---|---|
| سال و تاریخ | فقط با جستجوی همان جلسه | همان‌جلسه چک می‌شود، اما **مجاز و حتی لازم** است (تولد، فوت، نوبل، …) |
| نقل‌قول مستقیم | عملاً ممنوع | **مجاز**، اما فقط جمله‌ای که همان جلسه متنِ دقیقش دیده/تأیید شده |
| لحن | معلمانه، دوم‌شخص | روایی، قصه‌گو؛ سوم‌شخص با هیجان و آب‌وتاب |
| احساس | خنثی و آموزشی | مجاز: تراژدی، طنز، حسرت، شگفتی |
| جزئیات شخصی | بیربط | **مرکزی**: خانواده، ازدواج، فرزندان و سرنوشتشان، بدبختی‌ها و خوشی‌ها |
| دقت علمی | فرمول و قیاس دقیق | تا حدِ «داستان درست باشد»؛ توضیح علمیِ سنگین لازم نیست |
| فرمول | با هشدار می‌آید | معمولاً اصلاً نمی‌آید (مگر یک فرمولِ مشهور مثل E=mc² یا معادلهٔ شرودینگر در حدِ نام بردن) |

**قاعدهٔ طلاییِ روایت:** «روایتِ هیجان‌انگیز از یک واقعیتِ تأییدشده» ✅ — «واقعیتِ ساخته‌شده برای هیجان» ❌. هر جا داستان «خیلی خوب» بود، طبق قوانین الزامی باید مشکوک شویم و منبع بگیریم.

---

## بخش ۵ — نقشهٔ زندگی: تمپلتِ اختصاصیِ ما (چه چیزهایی باید پوشش داده شود)

هر مدخل، یک **کارت اطلاعاتی** و یک **نقشهٔ روایی** دارد. کارت اطلاعاتی را (به‌صورت داده، جدا از متن) پر می‌کنیم تا بدانیم چه داریم؛ نقشهٔ روایی ترتیبِ قصه را می‌سازد.

### الف) کارت اطلاعاتیِ دانشمند (چک‌لیست داده)

| فیلد | توضیح |
|---|---|
| نام کامل + لاتین | برای متای `_scientist_en_name` |
| تولد و مرگ | کجا به دنیا آمد؛ کی و کجا درگذشت |
| خاستگاه و خانوادهٔ مبدأ | پدر/مادر چه کاره بودند؛ وضعیت مالی و اجتماعی کودکی |
| تحصیل | کجا درس خواند؛ استادانش که بودند |
| حرفه | چکاره بود؛ مسیر شغلی و نهادها (دانشگاه، مؤسسه، صنعت) |
| ازدواج(ها) | چندبار ازدواج کرد؛ با چه کسی |
| فرزندان | چندتا بچه داشت؛ **سرگذشت بچه‌ها چه شد** |
| همکاران و رقبا | با چه کسانی کار کرد، شاگردان و استادان، دوستی‌ها و دشمنی‌ها |
| اثر/کشف اصلی | یک جملهٔ روشن که چرا در فهرست ۵۰تایی است |
| جوایز | نوبل و غیره (با سالِ تأییدشده) |
| مشکلات | موانع، تبعید، جنگ، فقر، بیماری، سانسور، رقابت‌ها |
| خوشی‌ها و بدبختی‌ها | لحظه‌های اوج و فروپاشی — سوختِ روایت |
| نقل‌قول تأییدشده | ۱ تا ۲ جملهٔ واقعی برای Blockquote |
| میراث | چیزی که از او ماند |

### ب) نقشهٔ روایی (پرده‌های قصه — ترتیب پیشنهادی، نه اجباری)

1. **شروع از میانه یا از تناقض** (لید ضربه‌ای — مثل پلانک و بور).
2. **خاستگاه**: کجا به دنیا آمد، در چه خانواده‌ای بزرگ شد، چه چیزی راهش را عوض کرد.
3. **مسیر علمی**: چکار کرد، با چه همکارانی، در چه نهادهایی؛ کشف/اثر اصلی.
4. **در هم‌تنیدگیِ زندگی و علم**: مشکلات، رقابت‌ها، خوشی‌ها و بدبختی‌ها (اینجا اثراتِ تصمیم‌ها را در همان مسیر می‌گوییم، نه در انتها).
5. **زندگی شخصی**: ازدواج‌ها، فرزندان و سرنوشتشان — با همان لحن روایی، نه گزارش اداری.
6. **اوج و فرود پایانی**: آخرین سال‌ها، مرگ، و داوریِ تاریخ.
7. **میراث**: یک جملهٔ ماندگار که خواننده با خودش ببرد.

> قاعدهٔ صداقت در نقشهٔ روایی: ترتیبِ پرده‌ها آزاد است، اما **هر پرده باید از کارت اطلاعاتی تغذیه شود**؛ اگر خانه‌ای از کارت خالی بود (مثلاً ازدواج نکرده بود یا فرزندی نداشت)، همان را با یک جملهٔ صادقانه می‌گوییم یا حذفش می‌کنیم — **هرگز جاهای خالی را پر نمی‌کنیم**.

---

## بخش ۶ — تطبیق با خواسته‌های گوگل (SEO / E-E-A-T)

مدخلِ دانشمند یک فرصتِ بزرگ برای **اعتبار سایت (E-E-A-T)** است؛ چون آدمِ واقعی، نهاد، جایزه و منبع دارد. تطبیق با گوگل:

1. **عنوان (Title)** ≤ ۶۰ کاراکتر، فارسی، حاوی نام دانشمند، متمایل به کوتاه:
   - ✅ «ماکس پلانک؛ پدری که از فرزندش ترسید» · ❌ «زندگی‌نامهٔ کامل و جامع ماکس پلانک فیزیک‌دان بزرگ آلمانی برندهٔ نوبل فیزیک»
2. **توضیح متا (Meta description)** ۱۲۰–۱۵۵ کاراکتر، ۲ جمله (قلاب + پوشش). گوگل آن را از فیلد چکیده می‌خواند (`qpedia_auto_seo_meta_tags`).
3. **اسلاگ** لاتین کوتاه بدون stop word؛ هرگز تکراری با مدخل‌های موجود نباشد (ممیزی اسلاگ مثل مقاله‌ها).
4. **ساختار تیتر**: H2 برای پرده‌ها، H3 برای پرسش‌های پرتکرار؛ بدون H1.
5. **لینک داخلی ≥۵**: به مقاله‌های آموزشیِ مرتبط (مفهومی که کشف کرد) و به مدخل‌های دیگر دانشمندان (همکاران/رقبا) — شبکهٔ موضوعی می‌سازد.
6. **لینک خارجی معتبر (۱–۲)**: دایره‌المعارف بریتانیکا، NobelPrize.org، بایگانی دانشگاه‌ها، یا منبع رسمی زندگینامه — در بخش پایانی «منابع». این برای E-E-A-T حیاتی است.
7. **نشان‌های اعتبار داخل متن**: نهاد، جایزه، استاد/شاگرد — طبیعی و بدون بزرگ‌نمایی (گوگل ادعای اغراق‌آمیز را جریمه می‌کند).
8. **تصویر شاخص**: پرتره/کاور سری QPedia، ۱۶:۹، مثل مقاله‌ها.
9. **E-E-A-T انسانی**: چون این ژانر آزادتر است، مهم است که **نقل‌قول و عددِ غلط** ندهیم — یک عدد یا نقل‌قولِ جعلی در مدخلِ دانشمند، کل اعتبار سایت را زیر سؤال می‌برد.

---

## بخش ۷ — ساختار HTML تحویلی (آمادهٔ چسباندن)

```html
(عنوان در فیلد «عنوان» وردپرس — داخل بدنه نمی‌آید)
(نام لاتین → متای _scientist_en_name)
(چکیده → فیلد excerpt وردپرس)
(دسته‌ها → scientist_field و quantum_scientist_group)

<p>لیدِ ضربه‌ای — ۱ تا ۳ پاراگراف کوتاه با قلاب…</p>

<h2>پردهٔ ۱ — خاستگاه</h2>
<p>کجا به دنیا آمد، در چه خانواده‌ای…</p>

<h2>پردهٔ ۲ — مسیر علمی و همکاران</h2>
<p>چکار کرد، با چه کسانی… <a href="https://qpedia.ir/.../">مقالهٔ مرتبط</a> و <a href="https://qpedia.ir/scientists/.../">همکار</a></p>

<blockquote><p>نقل‌قول تأییدشده</p></blockquote>

<h2>پردهٔ ۳ — زندگی شخصی و فراز و فرود</h2>
<p>ازدواج‌ها، فرزندان و سرنوشتشان، مشکلات و خوشی‌ها…</p>

<ul>
<li>رویداد/فقدان/اوج ۱</li>
<li>رویداد ۲</li>
</ul>

<h2>میراث</h2>
<p>چه چیزی از او ماند…</p>

<h2>پرسش‌های پرتکرار</h2>
<details><summary><strong>۱. سؤال؟</strong></summary><p>پاسخ کوتاه.</p></details>
<details><summary><strong>۲. سؤال؟</strong></summary><p>پاسخ کوتاه.</p></details>

<h2>منابع</h2>
<ol>
<li><a href="https://www.britannica.com/...">Encyclopaedia Britannica — …</a></li>
</ol>
```

- تگ‌های مجاز: `h2 h3 p strong em blockquote ul ol li a details summary`. ممنوع: `div span style class` و هر CSS درون‌متنی.
- ترتیب پایانی ثابت: **میراث ← پرسش‌های پرتکرار ← منابع** (منابع همیشه آخرین بخش).
- طول راهنما: ۱۲۰۰ تا ۱۸۰۰ کلمه؛ کیفیت روایت مهم‌تر از شمارش است و متن با پرکننده آب‌بندی نمی‌شود.

---

## بخش ۸ — چک‌لیست نهایی پیش از تحویل

- [ ] عنوان ≤۶۰ کاراکتر، فارسی، حاوی نام دانشمند
- [ ] اسلاگ لاتینِ یکتا (ممیزی با مدخل‌های موجود و مقاله‌ها)
- [ ] چکیده ۲ جمله، ۱۲۰–۱۵۵ کاراکتر
- [ ] بدنه بدون H1؛ از H2 شروع می‌شود
- [ ] لینک داخلی ≥۵ (مقاله + مدخل‌های دیگر دانشمندان)
- [ ] ۱–۲ منبع معتبر خارجی در بخش پایانی «منابع»
- [ ] هر نقل‌قولِ داخل گیومه، همان‌جلسه تأیید شده (قانون ۱)
- [ ] هر سال/عدد، همان‌جلسه چک شده (قانون ۲)
- [ ] روایت‌های «خیلی خوب» مشکوک شمرده و منبع گرفته شده (قانون ۳)
- [ ] هیچ عدد حدسی‌ای قطعی جا زده نشده (قانون ۶)
- [ ] نیم‌فاصلهٔ صحیح در کل متن
- [ ] کارت اطلاعاتی (بخش ۵) پر شده — خانه‌های خالی گزارش شده، نه ساخته شده
- [ ] انتشار فقط پیش‌نویس

---

## بخش ۹ — مرزِ صداقت (نقل از قوانین الزامی، برای این ژانر)

این ژانر آزادتر است، اما چهار خطِ قرمز همچنان ثابت است:

1. **نقل‌قول جعلی ممنوع** — پرخطرترین عنصر. جملهٔ داخل گیومه فقط وقتی نوشته می‌شود که همان جلسه متنِ دقیقش دیده شده باشد؛ وگرنه نقل به مضمون بدون گیومه.
2. **سال و عدد از حافظه نیاید** — هر سالِ تولد/مرگ/نوبل/مقاله همان جلسه جستجو شود.
3. **روایت تاریخی، نه داستانِ ساخته‌شده** — اگر روایتی محل مناقشه است، یا ننویس یا منسوب کن («بنا بر روایت خودش در سخنرانی نوبل…»).
4. **روان بودنِ متن نشانهٔ درستی نیست** — زیباترین جمله‌ها بیشترین خطر جعل را دارند؛ زیبایی را از حقیقت وام بگیر، نه از تخیل.

---

## بخش ۱۰ — قالب پایهٔ جدید (مصوب ۳ شهریور ۱۴۰۵، پس از نمونهٔ اینشتین)

ساختارِ نمونهٔ «آلبرت اینشتین» که مالک به‌عنوان پایه داد، با این اصلاحات به قالبِ رسمیِ همهٔ مدخل‌ها تبدیل شد:

### ۱۰.۱ — گرید شناسنامهٔ سریع (رندر از قالب، نه از متن)
- اطلاعات پایهٔ دانشمند در **شبکهٔ گرید (Grid)** نمایش داده می‌شود، نه جدول.
- این گرید **توسط قالب** (`single-quantum_scientist.php`) از متاها ساخته می‌شود تا همهٔ مدخل‌ها یک‌شکل شوند؛ نویسنده آن را در متن نمی‌نویسد.
- **شش کادرِ ثابت گرید** (اطلاعات پایه‌ای که هر دانشمندی دارد، با همین ترتیب):
  1. نام کامل
  2. زادروز و درگذشت
  3. زادگاه و ملیت
  4. پایگاه‌های دانشگاهی
  5. جایزه و افتخارات علمی (جایگزینِ «جایزهٔ نوبل» + «مفاهیم و فرمول‌های جاودانه» + «دستاوردهای کلیدی در کوانتوم»)
  6. زندگی شخصی (جایگزینِ «خانواده»)
- نام لاتین فیلدِ هفتم نیست؛ زیرِ عنوان نمایش داده می‌شود (نه داخل گرید).
- چیدمان: **۳ ستون × ۲ ردیف** در دسکتاپ؛ در موبایل همان ۶ کادر به‌ترتیب زیر هم (یک ستون).
- خانهٔ پرنشده خودکار از گرید حذف می‌شود؛ جای خالی هرگز ساخته نمی‌شود.
- استایل: کارت‌های تیرهٔ سایبری با لیبل سبز نئونی (`--qp-neon`) — هماهنگ با پوسته.

### ۱۰.۲ — قلاب احساسی (الزامی)
- هر مدخل با یک **اپی‌گراف یک‌خطیِ ضربه‌ای** شروع می‌شود (تناقض/تصویر/غافلگیری).
- سپس یک **قلاب آغازین** (H2 پرسشی یا ادعایی) که می‌گوید چرا این آدم مهم است — با لحن هیجانی، نه گزارش‌گونه.
- مثال اینشتین: «او از میز کارمندیِ ادارهٔ ثبت اختراع، جهان را عوض کرد؛ و تا آخر عمر با جهانی که خودش ساخته بود، سرِ ناسازگاری داشت.»

### ۱۰.۳ — بدنهٔ روایی (فصل‌بندی)
- بدنه با H2های «فصل» روایت می‌شود، نه پرسش خشک.
- ترتیب ثابت: خاستگاه ← مسیر علمی و همکاران ← کشف/اثر اصلی ← چهرهٔ انسانی (ازدواج‌ها، فرزندان و سرنوشتشان) ← میراث.
- قیاس ملموس قبل از هر مفهوم دشوار (مثل «بلیت ۱۰۰۰ تومانی» برای اثر فوتوالکتریک).

### ۱۰.۴ — نقل‌قول مستند
- نقل‌قول‌های تأییدشده در `blockquote` با `cite` (نام، نامه/اثر، تاریخ).
- قاعدهٔ قانون ۱ قوانین الزامی همچنان حاکم است: فقط نقل‌قولِ تأییدشدهٔ همان جلسه.

### ۱۰.۵ — گاه‌شمار
- یک `ul.qp-timeline` از رویدادهای کلیدی (سال + رویداد) نزدیک پایان می‌آید.

### ۱۰.۶ — پرسش‌های پرتکرار آکاردئونی (جعبه‌ای)
- با `<details class="qp-faq"><summary>سؤال<span class="ic">+</span></summary><div class="body">پاسخ</div></details>` ساخته می‌شود.
- ظاهر: جعبه با حاشیه و سایه؛ با لمس، باز و بسته می‌شود (بدون جاوااسکریپت).

### ۱۰.۷ — ترتیب پایانی ثابت
قلاب (اپی‌گراف) ← گرید شناسنامه (از قالب) ← قلاب آغازین ← فصل‌های روایت ← نقل‌قول ← زندگی شخصی ← میراث ← کاوش بیشتر (لینک داخلی ≥۵) ← گاه‌شمار ← پرسش‌های آکاردئونی ← **منابع (۱–۲ منبع معتبر، همیشه آخر)**.

### ۱۰.۸ — فایل‌های پیاده‌سازی (افزونهٔ استاندارد)
- `qpedia-scientist-template.zip` — افزونهٔ استاندارد وردپرس (نصب یک‌کلیکی): قالب تک‌مدخلی (`templates/single-quantum_scientist.php`) از طریق `template_include` جایگزین می‌شود + متاباکسِ ورود فیلدهای گرید + استایل (`qpedia-scientist.css`).
- نمونه‌ها: `einstein-sample-preview.html` (پیش‌نمایش) و `einstein-content.html` (بدنهٔ وردپرس).
