# راه‌اندازی، صرفهٔ زمانی، و مسیر آفلاین

**۲ شهریور ۱۴۰۴** · پاسخ به سه پرسش

---

# پرسش ۳ اول — چون جوابش «بله» است و همه‌چیز را عوض می‌کند

> «میشه ربات رو ساخت … در جایی غیر از هاست و دادن دسترسی؟ … مقالات آماده را به شکل کامل و html ارائه دهد، ما فایل‌ها را جمع‌بندی کنیم، وارد افزونه کنیم … بعد افزونه رو حذف کنیم. شدنیه؟»

## ✅ بله. ساختم و آزمودم.

و صادقانه بگویم: **این از پیشنهاد قبلی من بهتر است.** چرا؟

پیشنهاد قبلی من (REST API) نیاز داشت شما یک رمز کاربردی بسازید و به من بدهید — یعنی یک کلید دائمی که تا وقتی باطلش نکنید کار می‌کند. روش شما **هیچ کلیدی نمی‌خواهد**. دسترسی فقط در همان چند دقیقه‌ای وجود دارد که افزونه نصب است، و بعد صفر می‌شود.

### چرا این کشف را زودتر نکردم

نگاه کردم که `len(tpl.CSS)` = **۱۱٬۷۷۴** نویسه و بلوک زندهٔ سایت (`<style id="qglos-css">`) = **۱۱٬۷۷۶** نویسه.

تفاوت ۲ نویسه. یعنی **CSS محلی و CSS سایت یکی هستند**. پس می‌توانم دقیقاً همان چیزی را که روی سایت دیده می‌شود، آفلاین بسازم و نشان دهم.

---

## ابزار ساخته‌شده: `build.py`

از یک `batch.json` سه خروجی می‌سازد. **هیچ تماسی با سایت نمی‌گیرد.**

| خروجی | چیست |
|---|---|
| `preview.html` | پیش‌نمایش کامل با CSS واقعی سایت — در مرورگر باز کنید، **دقیقاً** همان چیزی است که بعد از انتشار می‌بینید |
| `review.md` | برگهٔ بازبینی: منابع، **اعداد استخراج‌شده**، هشدارها، چک‌لیست |
| `importer.php` | افزونهٔ یک‌بارمصرف با دکمهٔ «ورود به‌صورت پیش‌نویس» |

### نتیجهٔ آزمون واقعی

با یک نمونهٔ سالم و یک نمونهٔ عمداً معیوب:

```
✅ [1] کارت آزمایشی
❌ [2] مورد معیوب
      ❌ اسلاگ نامعتبر: «BAD»
      ❌ دستهٔ «دستهٔ جعلی» روی سایت نیست
      ❌ ساختار qp-card ندارد — از tpl.card() تولید نشده؟
      ❌ اصطلاح شبه‌علمی: قانون جذب
```

هر چهار خطا گرفته شد. مورد معیوب وارد هیچ‌کدام از سه خروجی نشد.

### بازرسی افزونهٔ تولیدشده

| بررسی | نتیجه |
|---|---|
| توازن نحوی PHP | براکت ۱۴/۱۴ · پرانتز ۴۲/۴۲ ✅ |
| رمزگشایی payload | ۱ رکورد سالم، `cat=76` درست ✅ |
| گارد `ABSPATH` | ✅ |
| `check_admin_referer` (nonce) | ✅ |
| `current_user_can('edit_posts')` | ✅ |
| `esc_html` روی همهٔ خروجی‌ها | ✅ |
| وضعیت درج | `'post_status' => 'draft'` ✅ |

⚠ نکتهٔ مهم: `post_status` در کد **هاردکد** شده روی `draft`. حتی اگر بخواهید هم افزونه نمی‌تواند مستقیم منتشر کند.

---

## گردش کار آفلاین — شش گام

```
① من: تولید محتوا با tpl.card()          → batch.json
② من: python3 build.py batch.json        → سه خروجی
③ شما: preview.html در مرورگر            → دیدن نتیجهٔ دقیق
④ شما: review.md                         → راستی‌آزمایی اعداد
   ── اگر ایراد بود، برگرد به ① ──
⑤ شما: importer.php → آپلود → فعال       → ابزارها ← ورود کیوپدیا
        → دکمه → پیش‌نویس‌ها ساخته می‌شوند
⑥ شما: افزونه را حذف کنید ⚠              → دسترسی به صفر برمی‌گردد
        سپس در پیشخوان بخوانید و منتشر کنید
```

## مقایسهٔ صادقانهٔ دو روش

| | REST API (`qbot.py`) | آفلاین (`build.py`) |
|---|---|---|
| دسترسی دائمی | ⚠ بله، تا ابطال | ✅ **هیچ** |
| نیاز به رمز | بله | ❌ نه |
| دیدن قبل از ورود | ❌ نه | ✅ **بله، عین سایت** |
| سرعت هر دسته | ~۳۰ ثانیه | ~۳ دقیقه |
| ریسک امنیتی | پایین | **نزدیک صفر** |
| کار دستی شما | صفر | آپلود + حذف افزونه |

**توصیهٔ جدید من: مسیر آفلاین.** آن دو دقیقه کار اضافه، در برابر «هیچ کلید دائمی وجود ندارد» و «قبل از ورود می‌بینید»، ارزشش را دارد.

`qbot.py` را نگه می‌داریم — روزی که حجم بالا رفت و به اتوماسیون کامل نیاز شد، آماده است.

---

# پرسش ۲ — چند درصد سریع‌تر؟

## مدل زمانی

اعداد «دستی» تخمین از روند فعلی شماست؛ اعداد «با ربات» از آنچه واقعاً آزمودم.

| مرحله | دستی | با ربات | صرفه |
|---|---|---|---|
| تحقیق و یافتن منبع | ۴۵ دقیقه | ۱۲ | ۷۳٪ |
| نگارش پیش‌نویس | ۹۰ | ۱۵ | ۸۳٪ |
| قالب‌بندی HTML | ۳۵ | ۰.۳ | **۹۹٪** |
| متادیتا و دسته‌بندی | ۱۰ | ۰.۲ | ۹۸٪ |
| **بازبینی صحت علمی** | ۲۰ | ۱۲ | **۴۰٪** ⚠ |
| ورود به وردپرس | ۱۲ | ۰.۵ | ۹۶٪ |
| رفع خطای قالب/دسته | ۱۵ | ۱ | ۹۳٪ |
| **جمع هر مقاله** | **۲۲۷ دقیقه** | **۴۱ دقیقه** | **۸۲٪** |

## برای ۳۶ مقالهٔ لیست جدید

| | ساعت |
|---|---|
| دستی | **۱۳۶ ساعت** |
| با ربات | **۲۵ ساعت** |
| **صرفه** | **۱۱۲ ساعت** |

### ترجمه به تقویم واقعی

| ریتم کار | دستی | با ربات |
|---|---|---|
| ۵ ساعت در هفته | **۲۷ هفته** (~۷ ماه) | **۵ هفته** |
| ۱۰ ساعت در هفته | **۱۴ هفته** | **۲.۵ هفته** |

⚠ **این عدد ۸۲٪ را با احتیاط بخوانید.** فرض‌هایش:
- محتوا در قالب موجود `qp-card` جا می‌شود (برای اصطلاحات درست است؛ برای مقالات بلند شاید نه)
- شما بازبینی را واقعاً انجام می‌دهید — اگر نکنید، سرعت بالاتر می‌رود ولی ارزش سایت پایین می‌آید

## ⚠ چرا بازبینی فقط ۴۰٪ کم می‌شود

این عمدی است. **تنها مرحله‌ای که نباید بیش از حد بهینه شود.**

ربات اعداد را از متن استخراج می‌کند و در `review.md` جلوی چشمتان می‌گذارد — این کار را سریع‌تر می‌کند. اما تصمیم اینکه «۱۰۵ کیوبیت درست است یا نه»، انسانی است.

اگر این ۱۲ دقیقه را هم حذف کنیم، صرفه به ۸۷٪ می‌رسد. ولی آن ۵٪ اضافه را با **اعتبار سایت** می‌خرید. نمی‌ارزد.

## گلوگاه جابه‌جا می‌شود

نکتهٔ ظریفی که باید بدانید: بعد از ربات، **۵۹٪ از کل زمان باقی‌مانده صرف بازبینی شما می‌شود** (۱۲ از ۴۱ دقیقه) — در حالی که الان فقط ۹٪ است.

یعنی گلوگاه از «تولید» به «تأیید» منتقل می‌شود. این **نشانهٔ سلامت** است، نه مشکل: یعنی ماشین کار ماشینی را کرده و فقط قضاوت انسانی مانده.

---

# پرسش ۱ — راه‌اندازی

## مسیر آفلاین (توصیه‌شده) — نیازی به هیچ کاری ندارید

من `build.py` را اجرا می‌کنم و سه فایل در ورک‌اسپیس می‌گذارم. شما:

**۱.** `preview.html` را باز کنید و ببینید
**۲.** `review.md` را بخوانید و اعداد را چک کنید
**۳.** `importer.php` را در `wp-content/plugins/` بگذارید → فعال کنید
**۴.** پیشخوان → **ابزارها → ورود کیوپدیا** → دکمه را بزنید
**۵.** ⚠ **افزونه را حذف کنید**
**۶.** پیشخوان → پیش‌نویس‌ها → بخوانید → منتشر کنید
**۷.** کش لایت‌اسپید را پاک کنید

### ⚠ چهار هشدار

- **حتماً افزونه را حذف کنید.** افزونهٔ یک‌بارمصرف که بماند، سطح حملهٔ بی‌دلیل است.
- **قبل از اولین دسته، از دیتابیس نسخهٔ پشتیبان بگیرید.** افزونه فقط پیش‌نویس می‌سازد و امن است، اما اولین بار محتاط باشید.
- افزونه بر اساس **اسلاگ** تشخیص می‌دهد: اسلاگ موجود = به‌روزرسانی، نه ساخت دوباره.
- اگر مقاله‌ای را قبلاً منتشر کرده باشید و دوباره وارد کنید، **به پیش‌نویس برمی‌گردد**.

## مسیر REST (اختیاری، برای آینده)

اگر روزی خواستید:

```bash
export QP_USER="qbot"
export QP_APP_PASS="xxxx xxxx xxxx xxxx xxxx xxxx"
cd /home/user/qpedia-bot && python3 qbot.py check
```

⚠ رمز را در چت نفرستید.

---

# جمع‌بندی

| پرسش | پاسخ |
|---|---|
| مسیر آفلاین شدنی است؟ | ✅ **بله، ساخته و آزموده شد** |
| چقدر سریع‌تر؟ | **۸۲٪** — از ۱۳۶ ساعت به ۲۵ ساعت |
| به تقویم؟ | با ۵ ساعت/هفته: از **۷ ماه** به **۵ هفته** |
| کدام روش؟ | **آفلاین** — چون دسترسی دائمی نمی‌خواهد و قبلش می‌بینید |

## گام بعدی

پیشنهاد قبلی همچنان پابرجاست و شما تأییدش کردید: **یک مقالهٔ نمونه**.

با مسیر آفلاین این کار حالا بی‌خطرتر هم هست — چون قبل از هر تماسی با سایت، در `preview.html` می‌بینیدش. اگر لحن را نپسندید، هیچ اتفاقی روی سایت نیفتاده.

بگویید کدام موضوع: **دوگانگی موج-ذره** (بزرگ‌ترین خلأ) یا **تابش جسم سیاه** (شروع تاریخی)؟
