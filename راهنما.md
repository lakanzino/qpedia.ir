# راهنمای بارگذاری مقالات با قالب یکسان

## گردش کار

```
شما فایل‌های ورد را آپلود می‌کنید
        ↓
پوشهٔ  /home/user/maghalat/khaam/
        ↓
من  tabdil.py  را اجرا می‌کنم
        ↓
HTML تمیز در  /home/user/maghalat/html/
        ↓
شما در پیشخوان می‌چسبانید (یا افزونهٔ وارد‌کننده)
        ↓
افزونهٔ qpedia-tpl.php قالب را هنگام نمایش می‌سازد
```

---

## ۱) چه چیزی را کجا آپلود کنم؟

فایل‌های `.docx` (یا `.txt` / `.md`) را در پوشهٔ زیر بگذارید:

```
/home/user/maghalat/khaam/
```

نام فایل مهم نیست — عنوان از داخل خود سند برداشته می‌شود.

> **دو فایل نمونه** الان آنجا هست تا مکانیزم را نشان دهد. با آپلود فایل‌های واقعی، آن‌ها را پاک می‌کنم.

---

## ۲) سند ورد را چطور بنویسم؟

| در ورد | تبدیل می‌شود به | کاربرد |
|---|---|---|
| سبک **Title / عنوان** | عنوان پست | فقط یکی، اول سند |
| سبک **Heading 1** | `<h2>` | بخش اصلی |
| سبک **Heading 2** | `<h3>` | زیربخش |
| سبک **Heading 3** | `<h4>` | زیرزیربخش |
| متن معمولی | `<p>` | پاراگراف |
| فهرست گلوله‌ای | `<ul>` | — |
| فهرست شماره‌دار | `<ol>` | — |
| سبک **Quote** | `<blockquote>` | نقل‌قول |
| **پررنگ** / *مورب* | `<strong>` / `<em>` | — |

**اگر سبک‌ها را به کار نبرده‌اید** و همه‌چیز متن ساده است، اشکالی ندارد — بگویید تا از روی الگوی متن (خط کوتاه بدون نقطه = تیتر) حدس بزنم. فقط دقتش کمتر است.

### چیزهایی که خودکار اصلاح می‌شود
- `ي` `ك` عربی ← `ی` `ک` فارسی
- ارقام عربی `١٢٣` ← فارسی `۱۲۳`
- فاصلهٔ اضافی پیش از `، . ؛ : ؟`
- کشیدهٔ تزئینی و کاراکترهای جهت‌نمای نامرئی

### چیزهایی که حذف می‌شود
- تصاویر داخل ورد (جدا آپلود می‌کنیم)
- رنگ، فونت، اندازه و هر استایل دستی
- `<h1>` داخل بدنه — چون وردپرس خودش عنوان را `h1` می‌کند

---

## ۳) قالب چه چیزهایی اضافه می‌کند؟

روی **هیچ‌کدام از این‌ها** لازم نیست کاری بکنید — خودکارند:

| بخش | منبع |
|---|---|
| **کارت معرفی** | عنوان + چکیده + زمان مطالعه + چیپ حوزه |
| **فهرست مطالب** | از `<h2>`ها ساخته می‌شود؛ زیر ۳ تیتر نمایش نمی‌یابد |
| **لنگر کنار تیترها** | برای کپی لینک مستقیم به هر بخش |
| **برجسته‌سازی بخش فعال** | هنگام اسکرول، آیتم فهرست سبز می‌شود |
| **مقالات مرتبط** | هم‌حوزه، خودکار |
| **قبلی / بعدی** | خودکار |

### چهار فیلد اختیاری

اگر خواستید دقیق‌تر شود، در ویرایشگر پست بخش **فیلدهای سفارشی** را باز کنید:

| کلید | مقدار | اثر |
|---|---|---|
| `qpt_en` | `PHOTOELECTRIC EFFECT` | عنوان انگلیسی زیر تیتر کارت |
| `qpt_lead` | یک پاراگراف | جایگزین چکیدهٔ خودکار |
| `qpt_nokat` | هر خط یک نکته | جعبهٔ سبز «نکات کلیدی» |
| `qpt_terms` | `photon, laser, quantum` | چیپ اصطلاحات مرتبط (اسلاگ‌ها با ویرگول) |
| `qpt_scientists` | `albert-einstein, max-planck` | چیپ دانشمندان مرتبط |
| `qpt_off` | `1` | این مقاله را از قالب مستثنا کن |

> فیلدهای سفارشی پیدا نمی‌شوند؟ در ویرایشگر بلوکی: سه‌نقطهٔ بالا-راست ← **ترجیحات** ← **پنل‌ها** ← **فیلدهای سفارشی** را روشن کنید.

---

## ۴) نصب افزونهٔ قالب

فایل `qpedia-tpl.php.txt` را به `qpedia-tpl.php` تغییر نام دهید و در
`/wp-content/plugins/qpedia-tpl/` بگذارید — یا محتوایش را (بدون `<?php` اول) به انتهای `qpedia-shell.php` بچسبانید.

سپس **LiteSpeed ← Purge All**.

---

## ۵) چرا «در زمان نمایش»؟

چون شما گفتید ۱۰۰ مقاله. اگر قالب داخل خود محتوا پخته شود:

- تغییر یک رنگ = ویرایش دستی ۱۰۰ پست
- محتوای خام پر از `<div>` می‌شود و ویرایشش سخت است
- انتقال به قالب دیگر در آینده تقریباً غیرممکن

با روش فعلی، **یک فایل** را عوض می‌کنید و هر ۱۰۰ مقاله همان لحظه به‌روز می‌شوند. محتوای دیتابیس هم تمیز و قابل‌جابه‌جایی می‌ماند.
