# گزارش اسکن محتوای نمایشی سایت — ۲۰۲۶-۰۸-۲۶

## دامنهٔ اسکن
اسکن با User-Agent گوگل‌بات انجام شد و این موارد بررسی شدند:

- صفحهٔ اصلی: ۱
- آرشیو دانشمندان: ۱
- دسته‌های کوانتومی: ۱۴
- مقاله‌های منتشرشده `quantum_article`: ۴۰
- دانشمندان منتشرشده `quantum_scientist`: ۲۳

**جمع کل صفحات اسکن‌شده: ۷۹**

همهٔ ۷۹ صفحه در این اسکن پاسخ `HTTP 200` دادند.

## نتیجهٔ کلی
- روی **صفحهٔ اصلی، آرشیوها و اکثر مقاله‌ها** مشکل نمایشی خام پیدا نشد.
- بعد از انتقال حدود ۲۵ مورد به زباله‌دان، به‌نظر می‌رسد بخش زیادی از موارد خراب جمع شده‌اند.
- اما هنوز **۱۷ صفحه** با نشانهٔ محتوای خام/قدیمی دیده شدند.

### تفکیک مشکلات باقی‌مانده
- **۵ مقاله** با CSS/Schema خامِ نمایشی در متن
- **۱۲ صفحهٔ دانشمند** با shortcode خام `[qsci_card]` در متن

## ۱) مقاله‌های دارای CSS/Schema خام
این ۵ URL هنوز متن خام شبیه CSS یا JSON-LD را در خود صفحه نشان می‌دهند:

1. `https://qpedia.ir/lissajous-curve/`
2. `https://qpedia.ir/quantum-alternative-medicine-science/`
3. `https://qpedia.ir/spot-pseudoscience-one-sentence/`
4. `https://qpedia.ir/quantum-cryptography-internet-security/`
5. `https://qpedia.ir/mri-quantum-origin/`

### الگوی مشکل
در این صفحه‌ها مواردی مثل این‌ها به‌صورت متن دیده می‌شوند:
- `@font-face`
- `font-family:`
- `background:`
- `border-right:`
- `"@context": "https://schema.org"`
- سلکتورهایی مثل `.quantum-...`

### برداشت فنی
این‌ها معمولاً یکی از این دو حالت‌اند:
1. محتوای مقاله واقعاً CSS/Schema خام را داخل `post_content` نگه داشته است.
2. نسخهٔ نهاییِ پاک‌سازی `single-quantum_article.php` هنوز روی سایت اعمال نشده یا کش قدیمی مانده است.

## ۲) دانشمندانی که `[qsci_card]` خام نمایش می‌دهند
این ۱۲ صفحه هنوز shortcode خام را نشان می‌دهند:

1. `https://qpedia.ir/scientists/pascual-jordan/`
2. `https://qpedia.ir/scientists/hermann-weyl/`
3. `https://qpedia.ir/scientists/satyendra-bose/`
4. `https://qpedia.ir/scientists/hideki-yukawa/`
5. `https://qpedia.ir/scientists/freeman-dyson/`
6. `https://qpedia.ir/scientists/john-bell/`
7. `https://qpedia.ir/scientists/wolfgang-pauli/`
8. `https://qpedia.ir/scientists/paul-dirac/`
9. `https://qpedia.ir/scientists/isaac-newton/`
10. `https://qpedia.ir/scientists/niels-bohr/`
11. `https://qpedia.ir/scientists/max-planck/`
12. `https://qpedia.ir/scientists/hugh-everett/`

### برداشت فنی
- یا `single-quantum_scientist.php` روی سایت هنوز اعمال نشده/جایگزین نشده
- یا محتوای این دانشمندان به شکلی ذخیره شده که regex فعلی shortcode را کامل جمع نکرده

## جمع‌بندی اجرایی
### چیزهای تمیز شده
- اسکن امروز نشان می‌دهد cleanup قبلی بی‌اثر نبوده.
- ۴۰ مقالهٔ منتشرشده باقی مانده‌اند و فقط ۵ مقاله در اسکن امروز خام دیده شدند.

### چیزهای هنوز نیازمند اقدام
1. **۵ مقالهٔ HTML/CSS خام**
2. **۱۲ صفحهٔ دانشمند با `[qsci_card]` خام**

## پیشنهاد قدم بعدی
به‌جای اسکن عمومی دوباره، حالا باید targeted fix برویم:

### الف) مقاله‌ها
- نسخهٔ نهایی `single-quantum_article.php` که پاک‌سازی CSS/JSON خام دارد، روی سایت اعمال شود.
- بعد فقط همین ۵ URL دوباره تست شوند.

### ب) دانشمندان
- `single-quantum_scientist.php` بررسی/جایگزین شود.
- بعد فقط همین ۱۲ URL دوباره تست شوند.

## نتیجهٔ مدیریتی
پس از cleanup، وضعیت به‌وضوح بهتر شده؛ اما سایت هنوز «کاملاً پاک» نشده. مشکل باقی‌مانده عمومی نیست، بلکه **متمرکز و محدود** شده است:

- **۵ مقالهٔ خراب**
- **۱۲ دانشمند با shortcode خام**

این یعنی الان وقت اسکن گستردهٔ مجدد نیست؛ وقت **وصلهٔ هدفمند** است.
